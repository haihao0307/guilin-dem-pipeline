# 三地区真实地貌与水系工作台 v1.0

入口：`index.html`

页面并列显示桂林、温州、昆明三个地区。桂林直接加载仓库现有 12.5 m 真实高程。温州与昆明在各自精确 10 km × 10 km 裁片实体生成前保持锁定。

每个地区和共享区均支持文字输入、参考图上传、本地 IndexedDB 暂存与 JSON 入库包导出。正式知识路径位于 `knowledge/terrain-hydrology/`。

页面不包含植被运行时。
