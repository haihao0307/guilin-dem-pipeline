# Landscape Mother: water-eroded limestone study

One compact, self-contained numeric WebGL2 workbench. Candidate identity: limestone-water-2. User-authorized continuation of b0433cb5186f79db8ec0fc681338f2429a8cc2cd. The old candidate remains in Git history, not in the new runtime.

The 81,635-byte HTML has SHA256 f2a98883f348c1702b7f49cfa6b351c6f7f9917d87c9a29e672ef0c359b360e0. It contains functions and parameters, no stored mesh, external assets, texture sampling or LOD. Initial generation has a real CPU and memory cost. Default expanded upload data is 50,125,512 bytes; this excludes other browser memory.

The four-mass outcrop layout is retained conceptually, with rounded transitions, finite rotated/branched joints, gravity-directed reference-surface runnels and nested volumetric cavities. The default recipe contains 112 cavity cutters, including 78 nested cutters, and 20 accepted descending paths. These are geometric construction counts, not field observations or a geological solver. Smaller shader details change shading normals only. Six fixed presets (83,211,509,8231,9298,13007) modify actual geometry and can be cycled by the visible button. All use the same fixed sampling policy; enclosing field bounds change only to avoid empty computation, not to change resolution.

Brick source remains read-only: haihao0307/HOUSE@53a4b0728678e31ba4ebf2a9267a213597d8f226, nested brick-mother/experiments/atelier-r4/src/{kernel.js,renderer.js,catalog.js}. Its stone-only family functions and finite microscope material were retained. Four donor families each contribute six stones. No brick, adobe, fiber, source UI, external model or texture-shadow system is imported. Source mineral identity follows each fragment's reference coordinates, while final geometry and current position supply environmental proxies.

Support now queries the rendered soil triangles. Candidate orientation minimizes center-of-mass height over tested poses; all vertices, edge midpoints and face centroids are checked for ground contact. A bounded compliant-soil embedding is explicit, with the projected center inside the near-contact hull. Small burial is a modeling assumption, not interpenetration-free rigid dynamics. No fall trajectory, rolling, friction or geological age is claimed. Shadow queries use component-local collider bounds, fixing the old loose-fragment shadow footprint. All visible stones participate in this numeric visibility approximation.

Exact duplicate Float32 positions are merged, and exactly collinear facets are retriangulated using existing vertices. No remaining vertex coordinate is moved or quantized by that cleanup. Near-zero disconnected numerical dust is recorded before exclusion. This cleanup does not depend on camera or device. Normal orientation is repaired consistently; closed, two-incidence edge checks and zero-area checks are run on all six presets.

Local final-code checks: six preset generations passed actual soil-support sampling, oriented two-incidence edges, nondegenerate facets, finite/unit attributes, donor-family counts, enclosing-boundary samples and routing source/terminal bookkeeping. The routing ledger is a final-surface projected-area proxy, not calibrated water or soil mass conservation. Detailed checks and the clean rebuild sources accompany the source package.

Exact HTML document execution passed 32 desktop and 33 mobile-view checks locally in Chromium/SwiftShader, including seven views, all modes, native mouse/touch/pinch, wetness/material isolation, actual seed-button use, JSON export/import and exact geometry restoration. This local result used full-document loading, not HTTP navigation. The checked release workflow separately requires real HTTP byte identity and interaction on desktop and mobile before publishing and then repeats the checks at the public URL. Physical-phone/GPU performance is not approved.

Stronger directional light, cavity occlusion, mineral color layers, soil variation and larger moisture-associated moss patches are authored appearance candidates. Full Putao DEM, local geology, chronology, soil horizons and erosion rates remain unbound or uncalibrated. Numerical, browser and visual acceptance are distinct. visualApproved=false; productionReady=false.

Only this source namespace and the existing landscape-mother-workbench public subtree may be updated. The workflow verifies protected core/truth identities and leaves every other public root untouched. Temporary ASCII transfer chunks are verified against the complete tested HTML hash and are deleted by the existing freeze step after staged checks. No Make jobs, new public namespaces, force pushes or donor writes.
