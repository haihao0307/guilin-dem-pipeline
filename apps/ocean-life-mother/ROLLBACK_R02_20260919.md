# Ocean Life Mother rollback to R02

Date: 2026-09-19

The user rejected OLM-N02-20260919. It is not an accepted baseline and must not be resumed.

Completed rollback actions:

- Removed `.github/workflows/ocean-life-n02-public-smoke.yml` from `work/ocean-life-mother-r00-20260918`.
- Removed every tracked file under `apps/ocean-life-mother/nature-core/n02/` from the active source branch. The directory is now absent.
- Removed the public GitHub Pages namespace `ocean-life/n02-041c3800fdbe/` with gh-pages commit `632b664e82b01a6b894bd789866ac510170c0596`; the repository contents endpoint returns 404 for that path.
- Restored `apps/ocean-life-mother/VISIBLE_WORKBENCH_STATUS.json` to active version R02.
- Preserved the previous R02 source tree, including Bird access.

Production direction after rollback:

- Fish is the only immediate Ocean Life production focus.
- Do not reuse the N02 analytic toy-fish recipes, renderer, scene architecture, coral code or publishing workflow.
- Pause Coral work in Ocean Life Mother. Coral is delegated to Coral Mother until a separate accepted handoff exists.
- Preserve Bird from R02; do not remove it while rebuilding Fish Mother.
- Keep the supplied tuna references and make tuna a required fish target rather than omitting it.
- Start from actual fish body/head/mouth/eye/gill/fin relationships, source appearance observations and natural movement evidence. Do not hide fidelity problems by adding more generic coloured variants.
- Any next visible version must be visually reviewed before public release. Keep R02 active until that version is demonstrably better.

This deletion removes N02 from current source and public branch trees. Git history still records past commits; no history rewrite was performed.
