# Original Fish / Tuna R09R4 — high-dimensional primary shape gate

This candidate continues the R08 high-fidelity restart. It does not inherit the rejected low-dimensional Tuna R02–R10 body parameters.

- one continuous 260×144 body field reconstructed from exact source triangle-plane sections;
- ten independent UV-domain thin-plate RBF fin membranes;
- every source fin boundary control is retained in the independent triangulation;
- paired analytic eye volumes;
- no source GLB, texture, topology, 98-joint rig or animation track at runtime.

The fixed-view silhouette gate is provisional only. Final FISH 3A Q1 thresholds are not yet met, and jaw/operculum/fin-root anatomy remains on HOLD. Materials and motion remain CLOSED.

Local direct viewer: `candidate.html`. The remote repository stores the reproducible tool and QA. A remote browser payload is published only when all payload chunks are present and hash-verified.

Remote note: metadata, tests and reproduction tool may be committed before the large browser payload. Do not call the remote entry runnable until every payload chunk is present and the loader verifies the full candidate SHA-256.
