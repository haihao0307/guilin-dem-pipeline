# Landscape Mother active work

Work only on feature/landscape-mother-field-graph-v002 using normal fast-forward commits. Recheck the remote head before writing. The seven-file landscape-mother/ core remains the authority for numeric-only, fixed-geometry, zero-LOD, zero-texture rules. Read its AGENTS.md, SKILL.md and platform.json first.

The user's 2026-08-31 request authorizes a separate clean online workbench and several original procedural cases in workbenches/landscape-mother/. These examples have no regional truth binding or survey claim. Do not reload old demos, regional DEMs, archived prompts or old runtime implementations. Keep the cleaned core and shared truth, contracts, knowledge, pipeline and viewer byte-identical.

The only new publication authorization is the landscape-mother-workbench/ directory on the existing gh-pages branch through the scoped Studio workflow, after numeric and staged browser QA. All unrelated public paths, main, other branches, releases, PR 60 and Pages settings remain unchanged. The workflow may request an existing Pages build; it must not reconfigure Pages. This narrow consumer publication does not change the independent core's own unbound status.

Keep numeric checks, browser behavior, hardware performance and artistic approval separate. Browser screenshots may be stored as QA evidence only and never become runtime material assets. All visual and production approvals remain false until the user approves actual results.
