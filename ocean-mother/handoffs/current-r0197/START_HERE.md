# Ocean Mother 当前全量交接入口

包名：`Ocean_Mother_Current_Full_Handoff_R0197_2026-09-12.zip`

用途：把当前 Ocean Mother 的真实状态完整移交到新生产线，避免把移动端兼容候选误当成视觉母体，也避免丢失此前接近成功的海洋与海岛效果。

## 新生产线首先读取

1. `CURRENT_STATE.md`
2. `SOURCE_LOCKS.json`
3. `HANDOFF.json`
4. `METHOD_GUIDANCE_KAOPU_LEARN_R0.1.md`
5. `PACKAGE_SCOPE.md`
6. `PUBLIC_PREVIEWS.md`
7. `PACKAGE_CHECKLIST.json`

随后运行：

```bash
python verify_package.py .
```

## 当前必须保留的判断

- `R019.7 Mobile Heightfield` 已由用户在 iPhone Safari 上确认可以正常看见和操作，但视觉仍然太粗糙，尚未达到商业水准。
- R019.7 的角色是移动端兼容与运行诊断候选，不是新的视觉母体。
- `R018.11 / restart-v0311` 保留了此前接近成功的视觉路线及完整重启资料，应作为视觉恢复源，不得被 R019.7 的低清兼容外观覆盖。
- 下一条生产线的工作不是从 R019.7 的粗糙画面继续堆细节，而是从冻结视觉源恢复原效果，再逐项移植已经验证有价值的非阻塞启动、分阶段 shader、像素预算和 context restore 修复。
- 每一项移植都必须在同镜头、同参数下与冻结视觉源做 A/B；不能只因为运行更稳定就判定视觉更好。

## 下一阶段目标

形成一个商业方向的移动端候选，同时满足：

1. 固定 HTTPS 地址在 iPhone Safari 上可启动、连续运行和恢复。
2. 海岛主形、海面外观、近岸光学和深海谱场至少不低于冻结视觉源。
3. 不再出现灰色剪影岛、明显块状边缘或低清占位海面。
4. 近岸浪、泡沫、礁岩、深浅水和三处烟火逐项验收。
5. 海面外观、谱参数、物理状态、单位、尺度、时间和误差分别记录；视觉通过不自动证明水动力正确。

## 禁止事项

- 不得把 R019.7 标记为用户视觉批准版或生产完成版。
- 不得删除或覆盖 `frozen_visual_source/restart-v0311`。
- 不得因为模型或方法升级而恢复被禁止的外部贴图、模型或无关工具路线。
- 不得用 HTTP 200、本地 HTML、ZIP 或源码存在代替固定公网实际画面验收。
- 不得在没有同镜头 A/B 证据时重新发明岛体、主波谱或整体色彩体系。
