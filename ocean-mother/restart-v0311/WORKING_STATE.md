# 当前真实工作状态

当前候选 R018.11，运行身份 `0.3.11-island-r018-gpu-recovery`，构建 `r01811-bounded-gpu-context-recovery`。本包原样封存用户收到的 138544 字节 HTML，不再在打包时修改它。

R018.10 是上一公开修复候选。用户已反馈 WebGL 上下文丢失并显示整页错误。R018.11 在此基础上加入渲染像素预算、分块 GPU 工作、单任务等待、上下文恢复后资源重建及手动重启入口。与 R018.10 的近岸 FRAG/VERT 和内嵌深海文档一致性由本次静态检查确认。R018.11 的数值场缓存路径在现有代码中关闭，`fieldSupported=false`；不得把预留代码记成已启用优化。

继承报告 `evidence/R01811_RECOVERY_QA_INHERITED.json` 指向本包同一 HTML SHA。报告写明 `in-memory HTML; URL navigation not permitted in test browser`。桌面 1920×1080、DPR 1；移动 390×844、DPR 2。两组模拟丢失/恢复记录均为 1 次，恢复后参数、镜头和暂停状态保留；报告中 errors 和 externalRequests 均为空。该报告没有提供 R018.11 深海回归或真实 Windows GPU 验收。

报告最终软件渲染 FPS 约为桌面 0.044、移动 0.093，内部画布分别降至 340×191、173×375。它证明特定测试流程发生过恢复，无法证明可用流畅度。当前仍有明确性能阻塞，不得标记最终完成。

本次只运行打包相关完整性、来源锁定、重建和语法检查。新提供的 `tools/recovery_smoke.py` 是待复跑脚本，本次没有把它标记已执行。继承 QA 不冒充本轮重新执行。

待完成：解决用户设备上下文丢失与重负载；验证真实 URL 和离线直开；保持清晰度并提升连续帧率；改善像素块状泡沫和不明显的卷浪；复核深海原版及往返；确认移动端面板、底栏不遮挡主画面。全部视觉与生产批准维持 false。

打包时 GitHub 原工作分支 HEAD 为 `00329cd78bfeb5bc8b5a1061a1ad6f5154dcdab5`。公开站点历史源固定 `5cbef6aa5469de6dd729cae3b89a4694c5a39d62`。本轮不更改网站部署，R018.11 公开发布继续 pending。
