# Ocean Mother 接管规则

本文件适用于本次 V0.3.11 全量重启快照。历史 AGENTS 原文另存于 `history/`，其中早期分支、轮次及交付状态不覆盖本文件和用户最新指令。

默认生产和交付可交互的三维 HTML、WebGL、几何、着色器与数值系统。仅用户明确提出图片需求时才处理图片需求。生成图、静态截图、概念画不作为功能、进展或视觉完成的证据。内部浏览器截图仅为 QA，不作为用户交付物；本包不含截图或图片资产。

保留岛体、沙滩、石头、火焰、烟雾、镜头预设、旋转操作和用户已要求保留的布局。未经许可不得重做这些内容。水面、泡沫、卷浪可在独立候选内继续改善。修复启动、显存、上下文恢复时先记录精确基线并检查越界差异。

深海冻结为原始 V001。包中 `frozen-deep/Original_Deep_V001.html` 与当前 HTML 内嵌文档一致；原模块源码与天气依赖也已保存。禁止用新场景替换旧深海。改变深海视觉、参数或算法需要用户另行授权。

运行时禁止持久图片贴图、噪声图片、法线图片、截图背景、预烘焙环境图片、外部模型和 CDN。临时 GPU 帧缓冲与数值缓存须与资产区分，不能宣称其等同实测数据或守恒物理。

本次 GitHub 操作仅归档到 `handoff/ocean-mother-full-20260905-v0.3.11`。不强推、不改历史、不合并 main、不改其他 Mother，不覆盖 gh-pages 或旧工作台。重新研发时由本快照开独立工作分支。

测试按来源和类型分开记录：本次打包静态检查、继承的恢复报告、真实浏览器、URL 加载、公开发布、实机性能、用户批准各有独立状态。禁止把函数名、标记字符串、成功提交或 HTTP 200 当成画面质量与持续运行通过；禁止降低门槛掩盖故障。继承报告保持原件，补充说明写新文件。

重新开工先读本包顶层入口与状态，再读历史知识原文：`history/v030/knowledge/skills/ocean-math-physics/SKILL.md`、`history/v030/knowledge/contracts/`、`history/v030/governance/adoption/`、`history/v030/governance/bridge-v1/UPSTREAM_LOCK.json`。历史来源的理论与既有限制原样保留，不把接收资料写成运行实现。

所有视觉、生产、硬件批准继续 false，直到对应精确构建有真实验收记录。优先修复实际主画面，避免耗时在无关展示、重复包装或流水线循环上。
