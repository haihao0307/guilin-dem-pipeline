# Codex 后续任务：接入与首个数值波面

本文件仅登记后续执行任务。本轮用户要求知识蒸馏，未要求立即执行下述渲染与物理开发。

## 开始条件

只在 work/ocean-mother-handoff-20260901 工作。知识整理前基线为 f85f0332f4056dc401e7186a7895114cc53dbf33；执行时重新读取最新远端并正常快进。先读 ocean-mother/AGENTS.md、README.md、skills/ocean-math-physics/SKILL.md、contracts/OCEAN_KNOWLEDGE_CONTRACT.json、bridge-v1/README.md、bridge-v1/UPSTREAM_LOCK.json 和 HANDOFF_ACCEPTANCE.json。

先运行知识静态门禁与其自测。未经新的明确任务，不开始海面、船只或刚体求解器实现。不改 main、gh-pages、冻结天气、DEM 或其他生产线；不新建 PR、不合并、不强推、不覆盖历史。

## O1B 接入验证

从交付 ref 2619725efe236d2df8f2a55031bdae9e60a51555 取得精确天气原件，按锁核验六个运行文件和 MANIFEST 的 files 字段。旧 repositoryReadRef 不用于定位。测试副本原字节保留。

重跑现有桥接测试，源函数测试通过 WEATHER_ENGINE_PATH 指向冻结 engine.js。真实浏览器启动同源宿主与天气页面，使用现有 EnvironmentBridge，不能再写第二套桥接。

覆盖四种来风方向；风云速度独立；显式 windLink；阵风不重复乘；暂停恢复；时间倍率；同刻重复采样；时间回退；向前配置载入；来源丢失与恢复。配置载入前由宿主显式 resynchronize。

ready、种子、云属与 blend 检查只证明现有参数层条件。同种子同云属下密度重建的全部 GPU 中间状态仍不可由现有 API 完整识别，保留此限制，不宣告共享资源已就绪。

记录精确源码身份、加载 URL 与字节哈希、实际浏览器与后端、视口、DPR、控制参数、consoleErrors、pageErrors、failedRequests 和真实截图。导航或 WebGL2 阻断时保留真实错误，不声称通过。

## O2A 数值波面

仅在获得实现授权并通过 O1B 后执行。暂不增加船只、岸线、泡沫、海底、潮汐、完整云反射或后端迁移。

沿现有 WebGL2 / GLSL 路线隔离制作单波实验。状态只保存种子、米制参数、时钟与几何规则。禁止加载、内嵌、生成或保存图片贴图；法线由位移导数得到。全部外部产品和教程名称从生产界面及文档排除。

依 OM-M02 建立共享波面查询和真实几何。水位与波形分离，omega/k 不当作风速或水流速度。没有水流模型时 waterVelocityMps=null。CPU/GPU 使用同一配置与明确误差预算，物理和渲染需查询同一时刻。

依 OM-M01 设计固定物理步长、时间戳输入和重同步边界。初版不实现刚体求解器，先验证接口。依 OM-M03 使用稳定世界坐标，不随相机重置波相位与种子。

验证零振幅、峰谷差、传播方向、解析法线、暂停、同种子复现、原点重定位及网格加密收敛。提供线框、法线和中性光模式。仅一个波组实现时，其他控制保持 disabled/unimplemented。

## 物理知识使用边界

OM-P01 至 OM-P04 为后续独立任务的规则。实现顺序建议：静水平面与解析浮体；准静态多点浮力与力矩；有明示水流的阻力；简单刚体接触；高速连续检测。不得将波面跟随动画标为浮力，也不得将包围盒重叠标为物理响应完成。

## 收尾

全部测试输出与能力声明必须区分知识、静态门禁、数学、浏览器、性能、公开部署和视觉批准。准静态近似单独标注。截图走证据 artifact，不进入运行资产。普通 push 后回读 head 和变更范围，公开部署另有受控任务。所有人工批准保持 false。
