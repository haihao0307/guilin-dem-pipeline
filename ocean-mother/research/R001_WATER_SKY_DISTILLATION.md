# R001 方法迁移记录

R002 已将上一轮资料笔记收敛为 skills/ocean-math-physics/SKILL.md 与 contracts/OCEAN_KNOWLEDGE_CONTRACT.json。当前文件移除外部产品名、课程名、演示名、作者名、原始链接与商业接口清单，保留可复用方法及原有证据边界。Git 历史不改写。

上一轮性能文章全文没有取得；只能确认有限检索片段。课程只读取公开预览，付费部分未读取。这些限制保留，不能由本轮通用公式补齐成全文已读。

继承的方法包括：先验证单波真实几何，再增加尺度；网格由波长和误差约束；法线与位移共用函数；涌浪与本地风浪分开；平均水位独立；颜色渐变只辅助诊断。

继承的渲染规则包括：共用太阳方向与线性色；曝光和显示转换只由一个责任层执行；当前环境参数接口不代表共享相机、深度或云反射已经实现；分屏调试不能作为水天合成通过证据。

继承的性能规则包括：记录屏幕输出、更新频率、采样成本与缓存有效性；缓存跟随依赖变化失效；降低时效和分辨率须明确代价；先保存可比证据，再做优化。

当前无图约束优先。上一轮任何环境图、法线图或预烘焙资源做法均不作为资产生产路径。运行期临时深度和帧缓冲按内部数值资源管理，禁止导出成材质图片。

后端迁移须另立任务。当前沿 WebGL2 / GLSL 做隔离实验，不能假定另一后端接受相同自定义材质。确需第三方基础库时，另行核对版本与许可，本轮不引入依赖。

来源保留为中性编号和定位哈希，原始可点击引用留在研究对话。该哈希只能校验给定地址，无法独立还原地址。本项目拥有的是整理后的技能规范，通用数学物理规律不声明为独家发明。
