# 三地区共用资料收件箱

用于接收同时适用于桂林、温州、昆明的原始参考资料。

每批资料至少保留：

```text
intake ID
原文件名
上传时间
来源说明
用户备注
适用地区
主题标签
文件 SHA256
```

建议主题标签：`terrain`、`terrace`、`hydrology`、`riverbank`、`erosion`、`sediment`、`historical`、`shared-method`。

本目录保存原始资料和收据。规则提取结果写入相邻 `distilled` 目录。
