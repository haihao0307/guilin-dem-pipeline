# 小妈蒸馏主入口：统一函数体系与海洋核心

日期：2026-09-06。状态：用户已明确蒸馏方向；以下为自有设计草案，尚未实现、标定或完成产品验证。本轮只更新协调文档，不改动各生产分支、真实DEM、权威资产、公开工作台或人工验收状态。

## 用户本轮确认的目标

吸收不同年代、不同作者方案中的有效方法，统一为自己的思想与实现体系。最终工作系统只保留选定的核心、必要参数与状态，不把所有历史示例长期并列为生产方案。八、十、十六等层数可以按表现需求扩展；关注紧凑表达能够生成的丰富程度及关系正确性，不把层数当作固定架构。

从现在起，海洋函数化研究先读本页，再按具体证据问题查旧技能卡。旧来源卡属于研究依据，没有自动获得生产准入。其他Mother的既定职责与有效任务不变。

## 一套核心的定义

共用一套空间、时间、对象身份、字段含义和更新规则。不同尺度的波、扰动和材料函数是这个体系内部的可组合项；不为每个作者保留一套不同的海洋世界。

拟保存的最小内容是：生成规则及版本、地区边界与必要观测、参数和种子、无法从即时输入重建的状态或检查点。噪声层可以复用同一实现，改变尺度、幅度和相位；重复代码不随层数增加。是否需要更多层由对照效果和成本决定，先解决主形与作用关系。

输入分成地区约束、外部驱动和历史状态。地区约束包括海岸、海底及河口位置与可信范围；外部驱动包括潮位/潮流条件、风、外海输入和河流流量；历史状态包括泡沫、输运中的淡水/盐度或悬沙等确实需要记忆的量。未知的地区输入明确缺失，不能由生成噪声假装已经观测到。

拟设只读查询 sampleSurface(x,z,t) 返回水面位置、法线、表层流动和显示所需信息；sampleVolume(x,y,z,t) 按当前已实现模型查询水下流速与水体属性。两个接口读取同一个地区、版本、时间和状态。坐标单位、潮位基准、参数面与世界面的变换、数值容差都需显式约定。外观、交互和浮动物体使用同一水面定义，不能各自维护一套互相偏离的高度函数。

这只是接口设计。没有实现的查询返回未支持或未知，禁止用零值冒充真实流速、盐度或海底测量。

## 从模块拼接转向关系组合

基本组合操作包括相加、调制、坐标变换、求导、输运、生成和耗散。每个操作声明输入输出及其改变的性质。

波分量可在适用范围内叠加；潮位和海底共同决定局部水深；水深与流动参与波传播和破碎条件；破碎产生泡沫；表面输运与混合影响泡沫的分布；新泡沫的产生停止后，已有泡沫仍按状态规则演化。淡水输入与盐度、浑浊度、密度和运动的关联需要明确采用的近似，不能只把淡水画成一块独立颜色。

视觉细纹、域扭曲和旋涡细节叠加在这些关系上。若要求零散度、连续性或边界不穿透，组合后要重新检查；单个函数的证明不自动覆盖后续归一化、坐标扭曲或强度调制。艺术表现可以使用不同约束，须在输出语义中区分。

若采用不同频带，先约定相交频段如何分配能量，避免简单加层重复计算同一部分。大尺度背景与近景细节的求值成本分别控制，不能因相机移开就重置物理历史。

## 温州海域的拟研究关系

外海输入、陆架/沿岸背景、河口与岛岸局部过程采用同一接口描述，但空间尺度和时间尺度分别保留。黑潮相关影响拟作为有地区依据的外海或陆架边界输入，不凭一个局地旋涡函数宣称已重建黑潮系统。地方背景场可以采用经验证的低维表达；其参数、误差和有效时间范围需要从资料或实验中取得。

潮汐、风浪、背景流、河口淡水和近岸离岸流各有驱动，统一体系必须允许它们作用于共享状态。离岸流要检验波浪、岸形与近岸回流的关系；河口要检验水深、潮流、流量和盐度的关系。不能把所有现象只作为无关联的噪声层相加。

第一轮允许在明确标为合成场景的小区域验证这些接口。真实温州的重建与预测要另行标定，不能由演示逼真推定准确。既有真实DEM的canonical、数值、AOI、哈希和缺失标记不因本页改变。

## 按需计算与小体积

解析部分可以按位置与绝对时间求值。依赖历史的部分由显式advance/checkpoint流程恢复与推进，查询或拍摄画面不偷偷改变状态。可研究低维状态、有限局部更新和缓存，但没有可用检查点时不能任意跳过历史。层数可扩展，时间、显存和状态量必须有预算。

比较的体积包括核心实现、地方参数、必要观测及修正、状态和依赖，不能只统计一个短函数。目标是紧凑、可复现、细腻且相互一致；本轮不承诺最终字节数、跨设备逐位一致或无限细节的免费计算。

## 知识蒸馏与淘汰

先统一定义、单位、坐标、时间、版本与目标，再处理来源间的矛盾。旧知识可以有效，较新文章也可能只适用于特定工具或艺术表现；不能仅凭年代裁决。每项冲突以可观测预测和有区分力的检查判定。

工作入口保留我们当前选定的规则及已知限制。重复教程、未采用的第三方实现和失效候选不进入运行或交付依赖；只有仍有明确研究问题时才留在隔离区。来源、适用范围、必要署名/授权和一个代表性反例保留最小记录，不继续复制整套别人项目来维持并行路线。

现有研究材料先降为按需证据，本轮没有执行批量删除。后续删除使用具体文件清单并检查引用。已冻结交接原件、唯一恢复点和仍在使用的在线依赖不属于无用示例；迁移成功后再按现行批准范围处理。清理的目标是当前系统无冗余依赖，不以堆积旧方案证明学习量。

## 下一次验证只围绕同一核心

先用一个简单海面、一道岸形和一个河口输入建立有限样本，逐项改变风、潮位、地形或入流，检查共享波形、流动、泡沫和水体属性的响应。比较修改前后、固定时刻回放、不同显示帧率、相机移动、停止生成后的残留及新旧数据一致性。

验收同时看主画面质量、接触/输运关系、状态连续性与成本。书面架构成立不计为模型已实现，数学小算例通过也不计为真实温州或完整海洋验证。用户决定实际生产采用范围。

此前中断的curl-warp-composition/SKILL.md未写入成功，本轮不继续补成长篇逐项目说明。已保存的composition_probe.py暂留研究测试区；本轮未执行它，不填测试通过记录。其有用原理在本页统一为“组合后复核性质”的规则。

## 最小来源索引

S1 JangaFX，Curl Noise Explorations With IlluGen，实际读取正文中多尺度、输入形状和组合方法。其示例支持研究少量操作构成复杂外观；不证明真实海洋环流已重建。
https://jangafx.com/insights/curl-noise

S2 NOAA，What is a current? 与 What are tides?，实际读取正文，用于区分海流、风、密度及潮汐驱动。
https://oceanservice.noaa.gov/facts/current.html
https://oceanservice.noaa.gov/facts/tides.html

S3 SWAN技术文档，Spectral action balance equation，实际读取波传播、水深/流速变化、折射和源汇说明。采用其关系约束作参照，没有引入或运行SWAN，也没有把统计谱视为唯一确定一帧波面所需的全部相位信息。
https://swanmodel.sourceforge.io/online_doc/swantech/node12.html

S4 NOAA/NHC，Rip Currents，实际读取波浪积水与回流解释，仅作近岸关系来源，不作为任何海滩安全或预测工具。
https://www.nhc.noaa.gov/rip-currents/index.html

S5 Oujiang原研究的官方出版平台摘要：Modelling lateral circulation during flood season at the Oujiang River Estuary (2020) 与 High-resolution observations of lateral circulation at an inflection bend of the Oujiang River Estuary (2022)。本轮搜索索引摘要可读，原页面直接访问失败；只采用摘要支持的地形、潮阶段、流量和盐度影响，不声称全文模型、参数或实测数据已取得。
https://www.sciencedirect.com/science/article/abs/pii/S0141118720301346
https://www.sciencedirect.com/science/article/abs/pii/S027277142200186X

S6 Wu等，Modulation of Shelf Circulations Under Multiple River Discharges in the East China Sea (2021)，本轮读取出版页讨论与结论相关段落；用于支持河流、沿岸/陆架与黑潮作用应结合尺度研究。未读取全部图表或取得其数据，未将个案数值写成本项目标定。
https://agupubs.onlinelibrary.wiley.com/doi/full/10.1029/2020JC016990

本页为当前会话的蒸馏与设计成果；没有新增后台学习、定时调度、软件执行、其他Mother已读或产品批准。
