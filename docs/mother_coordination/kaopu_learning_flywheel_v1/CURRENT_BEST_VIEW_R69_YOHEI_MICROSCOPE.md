# Current Best View R69 — Yohei Macroscopic Microscope

Status: **Candidate partial**; the official historical repository host contract is observed, but applicability to the exact live artwork deployment and visual acceptance remain Unknown.

## What changed

- The source-history gate is now pinned to twigl commit `969491b285ba217fd895132a466ee6b3128243f3`, the latest repository commit returned at or before the 2025-01-18 artwork date.
- Its `geekest (300es)` wrapper declares inputs and `out vec4 o`, wraps `main` when absent, and preserves the user text. It does not assign `i/e/R/q/o`.
- GLSL ES 3.00 revision 6, the profile-exact Khronos source, confirms that those first reads produce undefined values.
- Repository code is MIT-licensed; the artwork license remains Unknown.

## Current method

The closest official historical twigl source does not provide the missing state. Any deterministic portable port must choose explicit seeds and remain a versioned reinterpretation until original deployment/runtime evidence establishes intended behavior.

Repository snapshot, deployed build, shader bytes, runtime pixels and human acceptance are separate gates. A framebuffer clear does not initialize the shader variable read by `o+=`.

## Preserved boundaries

- Article, host repository history and Khronos language specification remain distinct Observation Roots.
- The Node replay is a derivation, not a live twigl deployment, GPU observation or physical root.
- Exact deployed build, share URL, source bytes, dash repair, hardware/runtime and artwork license remain Unknown/Candidate.
- Gaussian R66 stays queued. Production Mothers, Canonical Truth and Frozen R1 remain unchanged.
- Mother routing is prepared only; no adoption is claimed.
