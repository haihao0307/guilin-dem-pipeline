{
  "schema": "kaopu-tool-routing/r71",
  "status": "candidate-routing",
  "routes": [
    {
      "tool": "Pinned historical twigl source plus public snapshot fingerprint",
      "use": "generate the full GLSL ES 3.00 program without storing the unlicensed artwork source",
      "gate": "snapshot SHA-256, mode, fragmen/noise Git blob SHAs and preprocessed-program SHA-256",
      "notProofOf": ["deployed recording bundle", "author GPU or pixels", "license"]
    },
    {
      "tool": "Chromium 143 / ANGLE Vulkan SwiftShader WebGL2",
      "use": "fixed-runtime RGBA32F and historical post-pass differential comparison",
      "gate": "runtime identity, compile/link logs, float and byte hashes, finite counts, clear and repeat controls",
      "notProofOf": ["GLSL-defined initialization", "cross-GPU equivalence", "hardware performance", "visual acceptance"]
    },
    {
      "tool": "Explicit zero-seeded reinterpretation",
      "use": "deterministic compact-versus-expanded differential reference",
      "gate": "version and hash separately from exact author source",
      "notProofOf": ["author intent", "original pixels", "physical surface process"]
    }
  ],
  "prohibitions": [
    "Do not republish raw artwork source while its license is Unknown.",
    "Do not describe implementation-specific equality as a GLSL zero-initialization rule.",
    "Do not count repeated SwiftShader runs as independent device roots."
  ],
  "productionChangesAuthorized": false
}

