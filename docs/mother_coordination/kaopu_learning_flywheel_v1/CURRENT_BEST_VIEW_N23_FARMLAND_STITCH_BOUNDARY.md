# Current Best View N23 — bounded procedural stitch boundaries

Status: **Candidate partial**

R045.36's contour-pair search and its drainage eligibility are separate contracts. At one fixed boundary a strong, identical contour pair exists on both epsilon sides, but `drain <= 0.035` returns the inherited field while the next representable side starts the stitch near full strength. The pinned replay measures a `0.0801605 m` height jump and `0.499299` mask jump across `0.000002 m`; R35 changes only `3.37e-7 m` there.

The transferable rule is boundary compatibility: if an eligibility boundary is intended to be visually continuous, apply its envelope to the **effect increment** and require that envelope to approach zero at the boundary. Smoothing a later score cannot repair an earlier hard return. Hard safety exclusions and intentional terrace risers may remain discontinuous, but must be named and audited separately.

The analysis-only taper proves the rule, not a production width. It reduces the fixed epsilon jump to the inherited numerical change while becoming exactly R36 inside the test band. Farmland must decide which predicates are hard safety locks and which are soft morphology eligibility, then return value, gradient/curvature, coarse mesh, fixed-view and existing lock receipts. No production algorithm or parameter is accepted yet.
