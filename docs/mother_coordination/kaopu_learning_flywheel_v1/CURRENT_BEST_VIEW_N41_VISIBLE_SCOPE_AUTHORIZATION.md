# N41 Current Best View — visible scope authorization

Status: `CANDIDATE_SINGLE_MOTHER_TRIAL_ONLY`

The R2 Task Anchor already has `targetObject`, `protectedInvariants`, `forbiddenRoutes`, `taskId`, `referenceSet` and a `scope_expansion` prohibition. The reference-replication gate already rejects creative substitutes. Those rules remain the baseline.

The uncovered failure is narrower: prose says “exactly one” and “no holes,” yet the runtime/source can add 19 instances and procedural pores without a machine-readable semantic comparison. Fresh bytes and a successful deployment do not answer whether that visible delta was authorized.

Candidate increment:

- Freeze `candidateClass`, reference digests, object-count bounds, allowed/forbidden visible feature types, `authorizationId`, and a canonical `scopeContractDigest` at dispatch.
- Have the producer emit an observed semantic inventory from the candidate source/runtime.
- Let a verifier compare the inventory to the frozen scope before promotion.
- If count, feature set, reference set, candidate class, or digest crosses the boundary, return HOLD and require a new Task Anchor/authorization. Do not silently edit the original contract.

Historical replay:

- The real R01/R02 line lacks a formal machine Task Anchor, so its machine state remains `UNKNOWN_TASK_ANCHOR_MISSING`.
- Reconstructing R01's explicit PR comment into the proposed fields admits the one-object method demo.
- Replaying R02 against that reconstructed contract returns `HOLD_SCOPE_EXPANSION_REQUIRES_REAUTHORIZATION` for count `19` and `procedural_blind_pores`.
- A counterfactual expansion with a different taskId and independently frozen scope passes only the scope-authorization gate. It does not become reference-faithful, production-ready, adopted, or user-accepted.

No-novelty: the need to freeze scope, reject creative substitution, and require authorization already exists. Novel Candidate value is limited to machine-readable visible cardinality/feature inventory, immutable scope digest, and explicit candidate-class transition semantics.
