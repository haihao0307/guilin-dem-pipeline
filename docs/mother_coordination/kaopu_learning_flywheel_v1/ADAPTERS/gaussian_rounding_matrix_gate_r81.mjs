let raw='';for await(const chunk of process.stdin)raw+=chunk;const d=JSON.parse(raw);
const required=['r80TableIdentity','calibrationMatchesFrozenR80','allPageChecks','calibrationContract','caseTargetLocks','allCasesIsolatedBeforeTarget','independentPrefixReadbacks','duplicateControls','allModelsEvaluated','outcomeClassified'];
const failures=required.filter(k=>d.checks?.[k]!==true);if(d.schema!=='kaopu-gaussian-rounding-matrix/r81')failures.push('schema');
const out={schema:'kaopu-gaussian-rounding-matrix-gate/r81',status:failures.length?'Candidate-fail':'Candidate-pass',failures,r80CenterTableHash:d.sourceLocks?.r80CenterTableHash,calibrationAgainstR80:d.calibrationAgainstR80,allThreeStagesRequired:d.allThreeStagesRequired,cases:Object.fromEntries(Object.entries(d.analysis||{}).map(([k,v])=>[k,{provenance:v.provenance,targetChannel:v.targetChannel,preTarget:v.preTarget,comparisons:v.comparisons,exactBothPrefixes:v.exactBothPrefixes,targetAblationRejected:v.targetAblationRejected,stagedExact:v.stagedExact,duplicateControl:v.duplicateControl,centerObserved:v.centerObserved,observedHashes:v.observedHashes}])),limits:d.limits};
process.stdout.write(JSON.stringify(out,null,2)+'\n');if(failures.length)process.exitCode=10;
