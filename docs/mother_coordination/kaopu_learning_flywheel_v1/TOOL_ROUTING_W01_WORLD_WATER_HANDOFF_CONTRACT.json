{
  "schema": "kaopu-tool-routing/1",
  "round": "W01",
  "status": "prepared-not-adopted",
  "routes": [
    {
      "tool": "Node.js CPU continuity probe",
      "purpose": "Schema, unit, ownership, source-support overlap and per-node/global conservation checks",
      "evidenceClass": "synthetic executable accounting fixture",
      "allowedClaim": "The declared W01 ledger fixture and counterexamples are internally reproducible",
      "forbiddenClaim": "KAOPU hydrology or runtime integration is physically validated"
    },
    {
      "tool": "Mother target runtime",
      "purpose": "Emit and replay real boundary fluxes on frozen supports and intervals",
      "evidenceClass": "required next gate",
      "allowedClaim": "none until executed",
      "forbiddenClaim": "adoption inferred from prepared routing"
    }
  ],
  "toolProhibitions": "No withdrawn tool route is restored; no production asset generation is authorized."
}
