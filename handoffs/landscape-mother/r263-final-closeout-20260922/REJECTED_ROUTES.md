# 禁止恢复的路线

## R2.6.4 cached production

用户此前已经否决。禁止作为当前默认架构、优化基线或恢复目标。

## R263-M1 / M1.1

属于本轮误交付的临时试验，不属于最终封版，不得从本包恢复。

## R263-M1.2 safe mesh

用户明确要求删除并退回原版。该路线以普通低质量网格替换连续函数场，视觉形体被破坏，禁止重新引入。

## 错误交付方式

正式入口不得再交付为下载后打开本地源 HTML。当前认可的入口是固定 GitHub 提交经 raw.githack 打开的在线工作台，并在包内以 `.url` 文件提供一按打开。
