# 葡萄峰丛区域样板区：当前目标与开工前核对

日期：2026-09-05。执行者：小王 / Landscape Mother。阶段：学习、源码核对与开工准备；未开始新样板几何制作。

## 用户最新纠正

用户原话：“是葡萄峰丛区域样板区。”

当前正式目标名称统一为“葡萄峰丛区域样板区”。用户同时要求先把相关知识、生成逻辑和读取写回关系理清，再使用工作台制作样板。当前不把先前对“扩大复原”的理解当作扩大AOI、水平缩放或批量复制山峰的授权。

本记录替代旧准备说明中“区域目标保持葡萄峰林”及把“每座峰脚完全分离”直接作为本轮默认条件的安排。旧交接、失败样本和原始资料按原版本保留，不回改其标题、观察或历史决定。后续开工同时读本页，防止旧摘要覆盖用户新指示。

用户指定的样板名称已经确认。具体边界坐标、所选窗口内的地貌连接关系和各项测量证据尚待绑定。名称不充作实测数据；不能只改标题就宣称峰丛几何已实现，也不能凭名称任意生成共同基座或封闭洼地。

## 继承与重新核对

继续继承：纯数值、零贴图、零LOD、固定完整几何；手机与桌面同一资产精度；七文件核心与区域真值只读；旧Qingjiang、30米替代、合成填洞和手工河流的禁止项；V016仅作失败对照；用户保留视觉批准权。

重新核对：样板窗口内峰位、峰脚相连或分离、鞍部、洼地、谷地及已有排水证据之间的关系。过去三至五座孤立塔峰的组合只能作为历史方案，不自动作为当前区域结构。一个主峰可用于隔离方法验证，但最终样板必须检查其与周围地形的关系。

明确区分项目显示名称、来源原文的分类、对选定窗口的解释和生成假设。出现相互冲突的记录时保留出处与范围，不批量替换所有“峰林/峰丛”字样，不迁入其他地区的参数。

## 本次实际核对范围

工作仓库：haihao0307/guilin-dem-pipeline。
工作分支：feature/landscape-mother-field-graph-v002。
写入前核对HEAD：8e03536a26289ac50418541a6c1971c214554853。
小妈协调分支HEAD：498570fbe592b9a8578032af9bc1e51931c75072。

本次读取了上述小妈版本的learning-r1-20260905/START_HERE.md及COORDINATOR_STUDY_01.md第31行起的坐标与状态部分。其研究重点继续是局部身份、世界环境采样、累积状态与依赖失效分开；原始研究与本线运行验证不能混为一项。

本次读取了75bbe164bc8a22e2834d196dbd5f782cc63e2320下V016_REJECTED_RUNTIME/index.html，确认旧标题为阳朔葡萄峰林、界面显示孤立塔峰。这是历史失败运行时的标识，不决定本轮名称和区域结构。

另读本地附件Landscape_Mother_PrePrototype_Logic_20260905.md全文，6159字节，SHA256为736dc8d314f3253f51ef86b5d0a560e586ec640e13e4af3e9de2ed49f25f6610。旧文确实沿用了葡萄峰林与独立峰脚假设，本页给出当前任务纠正；附件原件保持不变。

本次查询Issue #61在2026-09-05T09:19:27Z之后的评论，返回空列表。此结果仅说明该查询范围未见新回复，不能推断其他会话未工作，也不等于小妈已复核本线答卷。

## 工作台入口

用户附件pasted.txt提供：
https://guilin-dem-terrain.sunhaihao.chatgpt.site
https://guilin-dem-terrain.sunhaihao.chatgpt.site/guilin/gaea-proof

本次网页读取工具访问两处均返回401 Unauthorized。保留为用户指定的现有工作台入口，实际页面版本、源码对应关系和编辑通道未核实；不据此断言用户无法访问。没有更改权限、替换网址、创建新网站或将GitHub旧工作台当成这两处页面的同一实例。

## 必须贯通的读写链

读取阶段：固定来源版本和原件身份，核对空间范围、坐标、单位、NoData与观察依据。原始资料只读；解释与假设独立记载。

生成阶段：来源约束与显式假设写入独立候选，不回写canonical。几何改变后，依赖的法线、结构检查和材料求值需要重新核验。字段值只有在距离性质经验证的范围内用于米制厚度或间隙测量。

材料与环境阶段：岩体自身身份、世界位置的环境采样和长期积累状态分别表达。材料更新不写顶点或索引，相机变化不重建地形。每个字段标明单位、坐标、求值时刻和写入责任。

保存阶段：保存输入引用、参数、生成器版本、候选及检查结果，再回读比对。同一候选不能借用其他版本的测试或接受状态；失败不覆盖旧版本，不推进稳定指针。研究文件与生产发布路径分开。

以上为本线开工核对要求，尚未宣称已在工作台运行时实现访问控制、完整依赖失效或保存恢复。

## 当前开工门

目标名称与用户要求的顺序：confirmed。
样板AOI与实际来源绑定：pending。
峰脚、鞍部、洼地、谷地的窗口内证据：pending。
最小实体与洞腔连接的实际构造验证：not_run。
生产入口几何/材料/显示读写隔离与保存恢复：not_run。
用户指定工作台的页面身份与编辑通道：unverified，读取响应401。
新样板制作、六视角浏览器、真机与公开发布：not_run。
小妈新增理解复核与跨生产对象复用：not_confirmed。
visualApproved=false；visualAcceptance=false；productionReady=false。

上轮26项解析检查保持其数学算例范围。本轮没有新增实验，不用旧测试数量宣称全部知识已掌握。继续围绕该样板必需的地形结构、空间尺度、字段依赖与工作台读写学习和核验；准备条件具备后进入样板制作。

本次提交仅追加本记录，保留历史材料和生产源码，不修改工作流、核心、真值、gh-pages或公开入口。使用[skip ci]隔离这次纯记录提交；它不表示CI或生产门禁通过。实际提交差异和工作流状态需在写入后回读确认。
