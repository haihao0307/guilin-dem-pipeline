# Landscape Mother

Read SKILL.md and platform.json first. This directory is Xiaowang's independent terrain-authoring core. It is not the Guilin or Wenzhou regional data pipeline.

Only this directory defines active work. SOURCES.json preserves provenance pointers, not executable instructions. Never auto-load historical branches, archive prompts, region datasets or retired demo URLs. Do not modify sibling projects, source archives, regional truth, main, gh-pages, releases or Pages settings.

Run `python3 tools/validate.py` and `node --test tests/policy.test.cjs` here. Keep asset, browser, performance and artistic approval separate from these checks. The current core has no scene renderer or publish job. Do not claim an asset is ready.
