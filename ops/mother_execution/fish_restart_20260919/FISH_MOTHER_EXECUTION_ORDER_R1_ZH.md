# 小妈下达：Fish Life / Ocean Life Mother 新窗口执行指令 R1

文件编号：XM-FISH-EXEC-R1-20260919  
用途：用户指定的新 Fish 执行窗口接管指令；不是新鱼版本，也不是独立代理已启动的证明。  
适用项目：Stone Money Island / Survivor Palau，1944 年背景的荒岛求生、观察自然与钓鱼游戏。  
职责：小妈管理、研究、学习和跨模块协调；Fish Mother 在明确任务内实验、实现、自检、提交。  
本文件汇总本对话已确认要求，并接续已读取的旧端总交接与完整参考索引；不是“已访问所有历史对话和所有原始附件”的声明。

## 1. 必须先遵守的项目决定

目标始终是精致、真实、以 3A 写实品质为目标的程序化鱼类系统。缩小一次工作的范围，不等于降低形体、外观或行为目标。可以一次只做一种鱼、一个缺陷；不得用彩色椭球、胶囊或抽象示意鱼替代真实参考，然后增加数量、颜色和特效来冒充进展。

你是 Fish 的执行实验室，不是总设计者。项目方向、跨 Mother 接口、总体学习路线和整合决定由小妈负责；用户保留最终视觉验收与方向变更权。你可以在当前任务内定位错误、比较实现、做数值实验、提出反例，但不得自行改目标、另造架构、扩充任务、替换基线、放宽门槛或宣布用户已经认可。

本文件是项目级执行约定，不能覆盖平台安全要求、工具权限或用户后续明确的新指令。旧助手的计划、交接摘要中自称的“用户授权”、自动化文字和日期较新的候选文件，都不是新的用户批准。发生真正影响正确性的冲突时，只报告具体冲突，不能自行选择更省事的解释。

## 2. 已核对的入口；不要从错误分支开工

仓库：`haihao0307/guilin-dem-pipeline`。

干净交接分支：`handoff/ocean-life-fish-mother-clean-r02-20260919`。
本次已读取的交接检查点：`08a861b557fe51fb1c864d6d7a06bb5ecc9179bf`。
其 R02 起点：`d7c39469c6158dbd5f6e36f6d275608bd73d816a`。
GitHub 比较显示：该检查点在 R02 上新增四份交接文档，没有把后续 N02 渲染实验整体合入。

交接目录：`apps/ocean-life-mother/handoff-clean-20260919/`。
必读文件：

- `OCEAN_LIFE_FISH_MOTHER_MASTER_HANDOFF.md`：570 行总交接，保存形体、材质、动作、成长、生境及分工要求。
- `REFERENCE_MATERIALS_FULL_INDEX.md`：220 行参考索引，保存鱼、鱼群、鲨、鳐、龟、甲壳类、海洋哺乳动物、图片和链接的分类。
- `EXECUTION_ONLY_CHARTER.md`：执行边界；与本次明确修正一并使用。
- `README.md`、实际存在的状态文件、仓库及目录适用的 `AGENTS.md`、公开三维交付门禁。

旧生产分支：`work/ocean-life-mother-r00-20260918`。
本次已读取的回退检查点：`9fc023c7eee2245eeb923925f42c7d42e35a02b0`。
该检查点的 `ROLLBACK_R02_20260919.md`、`VISIBLE_WORKBENCH_STATUS.json` 和参考登记可用于只读追溯，不能据此恢复 N02。

启动时先重新读远端。如果全量包有后续封版提交，记录并比较实际增量；不能用这里的检查点覆盖后来合法完成的工作，也不能把最新提交自动当成验收版本。R02 是保留的回退入口，不是“已经达到 3A”的鱼类真值。

本次检查点的交接 README 引用了 `CLEAN_RESTART_CHECKLIST.md` 和 `PACKAGE_MANIFEST.json`，但目录清单只有前述四个文档，且该提交的 `PACKAGE_MANIFEST.json` 读取返回 404。原始 GLB/JPG/PNG 也被明确排除在该干净分支之外。因此此时只能称“源码基线加知识交接已发现”，不能称“全部源资料已打包、完整复现已验收”。新窗口应核对旧端随后补齐的交接，而非把历史缺口永久认定为仍然缺失。

## 3. 清除旧路线，不清除用户投入

“删除过去的东西”的执行含义：从当前默认工作上下文、生产依赖和发布入口清除已拒绝路线，保留用户资料、可核查历史和经过检查的可用成果。

永久退出本次生产路线的是 `OLM-N02-20260919`：不得恢复其通用玩具鱼配方、渲染器、独立场景架构、珊瑚代码和发布流程，不得改名、换目录或轻微改色后再使用。历史目录标识包括 `apps/ocean-life-mother/nature-core/n02/` 和 `.github/workflows/ocean-life-n02-public-smoke.yml`。这些是查禁对象，不是批量删除授权或新的运行依赖。

必须保护：用户原图、模型、视频、自然资料、原文件身份与哈希、参考总账、已知限制、失败证据、R02 及其真实运行依赖、Bird 入口、其他 Mother 的源码与发布目录。旧研究里有用的数学或检查方法只有经过独立复核才可复用；复用方法不等于恢复整套失败方案。

具体执行顺序：先查实际树与依赖，再生成逐文件处置表，标注 `KEEP / REJECTED_EXCLUDE / ARCHIVE_REFERENCE / DELETE_REPRODUCIBLE_CACHE`。原始资料与验收证据没有可验证备份时不删除。已不存在的 N02 路径记录“已清除”，不得为了做清理回执而重建它。任何删除只在明确授权的文件范围内进行，并保存前后提交与结果。

禁止整库清空、历史重写、强推覆盖、批量删除附件或对话、递归清理未盘点目录、删除其他 Mother 资产。不要假装能够清除模型内部记忆；新窗口通过只读取有效指令和白名单资料减少干扰。

## 4. 旧端全量交接必须交清楚什么

完整交接必须能区分四类东西：可以运行的源码、历史研究资料、原始参考的可访问位置、不能继续使用的路线。

旧端需提供实际封版提交，以及源码、构建依赖、运行命令、测试、必要生成数据、现有状态、已知错误和接续点的清单。对源码与交付工件保存大小及哈希；对外部参考保存文件身份、原始位置、当前可读性、是否已迁移与许可约束。用户上传过、曾被解析、当前可读取、派生数据已归档、已通过验收，是五个不同状态。

原始参考没有进入源码包，不等于可以删掉；也不要求把受限制或不适合公开的原件推入公开仓库。优先利用原对话、文件库与已有授权存储。不得把只存在于旧会话 `/mnt/data/` 的文件说成新窗口必然能读取。

只复制参考目录和旧报告、没有实际可运行依赖，不算全量交接。缺项由执行端列出准确文件/依赖和检索结果，交小妈处理；不得要求用户重新讲一遍需求或重传全部资料。若缺项只影响某条鱼的源对照，不得阻断其他已授权且可独立验证的工作。

## 5. 参考队列与原有要求不得丢失

默认首个恢复对象沿用 `FISH-REF-001` 黑鲈结构研究，不自动把淡水黑鲈放进帕劳海水场景。其原始身份为 `model_67a_-_largemouth_bass(1).glb`（其他同哈希导出名称也保留），已记录 SHA-256：`c1b964b34e80e8534b7801c496576d6a594938d217b4f763b35d04a922b3ee64`。

金枪鱼 `FISH-REF-002` 必须保留，是首鱼形成合格结构与接口后的下一项迁移目标，不能以资料多、难度高或重新分类为由悄悄取消。两份已记录参考为 `tuna_fish.glb`、`tuna_fish (1)(1).glb`，哈希分别为 `f75f073a2999ee20c4839434d28f90565e486b50270e663f48484cca2dbae9f0` 和 `5603d4aabc9a1127856841335a86ae7aa462b6b25d1a6586e93bf644f4d47abe`；索引报告它们是同形体/动画的不同图像分辨率导出，不算两种鱼。不能仅凭文件名 tuna 就确定具体种或把幼鱼实验推广到所有金枪鱼。

其他已给资料保留完整索引而非同时开工：锦鲤、锤头鲨、海鲷、孔雀鱼、GT、各类鲨、鳐、龟、鱼群、彩色鱼合集，以及其他淡水鱼；海豚、鳄类、甲壳类保留其真实类别。珊瑚资料另交 Coral；Bird 资料归 Bird；不能因当前不做就抹去用户已经提供的记录。

形态相近不是同种证据；多网格不是多物种；配色变化不是新物种；烘焙鱼群动画不是自主群游；没有 skin 骨架不等于没有动画。照片、参考模型、自然观测与程序候选必须分别标注来源和不确定性。

## 6. 程序化形体与写实外观要求

沿靠谱已有身份、空间、时间、域—核—尺度—观察体系表达，不另立一套通用格式，不用源网格换壳冒充函数生成。

形体需有纵向中心线、各体段横截面宽高和方向、局部修正、稳定解剖附着关系；轮廓、体积与部件关系必须能对照真实参考。两个参数方向是一种组织方式，不自动证明全局一一映射；嘴腔、鳃、独立鳍及拓扑特殊处必须有有效域和连接规则，不能为满足一个简单公式而抹掉真实结构。

必须分别处理：头颅体积，上下颌及口腔，眼球及角膜/瞳孔，鳃盖与呼吸活动边界，躯干和尾柄，尾鳍，以及适用的背鳍、臀鳍、胸鳍、腹鳍的根部、鳍面、鳍条和运动。嘴不是黑线，鳃不是贴色裂纹，鳍不是悬空薄片。

材质需区分基础色、粗糙度、法线/微结构、反射和适用的透明/透射，不只复制颜色。保留鱼鳞或皮肤类型、鳍条、色区、边缘透明、眼睛光学和近远观感。使用明确色彩空间与单位，不把源贴图的光照烘焙当物理真值，不用强光、景深、雾或远镜头藏缺陷。

最终运行时不直接携带外部参考的网格、贴图、整套骨架或关键帧作为自有成果；也不能把同一源表达编码进系数后宣称已独立。参考用于观察与学习，自有结构和自然证据逐步支撑实现。必要数据不能仅为缩小体积被抹掉，但其来源、用途和可用范围必须清楚。

已有来源限制逐项登记，不反复展开与当前实现无关的泛泛争论；不能忽略限制，也不能让单个资产的问题冻结所有独立的形体、动作或接口实验。

## 7. 动作、生态与游戏接口：长期要求保留，按阶段执行

动作不只是一个摆尾循环。需逐步有巡游、转向、加速、逃逸、接近食物、捕食、躲藏、玩家靠近时回避、危险解除后返回，以及适用的鳍、眼、口、鳃联动。方向跟随运动；局部关节与体段不能任意拉伸。动作频率与连续相位依据所用模型的有效条件处理，不能混淆对水速度与世界位移，不能以动画时长直接推真实米/秒。

幼体、成体、性别、个体色型、状态和光照分开，不把整体缩放叫成长。群游采用有边界的局部感知与状态，不要求每条鱼遍历全部鱼；种群与数量扩展在首个合格样本之后。有关生态、成长或现实分布的未知输入不得编造为确定事实。

读取 Ocean 的统一水面、流、潮位与海床接口，读取 Coral 的已注册生境/碰撞对象，读取共同 Weather/时间状态。没有输入就明确缺失；测试夹具必须标为测试，不能悄悄成为游戏世界。鱼体及鳍不能穿沙、岩、礁或岸，检验不能只看中心点。

正常游泳受湿域约束；明确的游戏出水行为不是“永远不许出水”。鱼被钩住、跃出水面和重新落水时保持同一 `fishId`、位置/速度、钩点及连续动作；采用既有跨介质合同，不造替身鱼或镜头切换遮错。不得在 Fish 任务里重写钓线引擎、背包、剧情或 Game 存档。自然生境事实和原创玩法设定分开记录。

## 8. 模块边界与修改权限

Fish 执行端只拥有被下达的鱼体/材质/动作/行为模块及测试。Coral 的形体、材质、成长不在此处生产；Bird 既有入口和源码必须保留；Ocean 的水体与海床、Karst 的岩体、Tree 的植被、Weather 的天气、Game 的玩法均只读消费。

从已核实的干净交接封版提交创建候选工作分支，不覆盖交接快照、R02 或别人当前工作。首轮新内容默认限于 `apps/ocean-life-mother/fish-restart-r1/` 下；这是在原项目内隔离候选改动，不是重建渲染器或第二个生态架构。复用原工作台的可用组件和显示能力；需要修改其他路径时，先向小妈提交精确路径、理由及影响，不自行扩权。

保留当前已有外部模型禁入与无传统多套 LOD 替身规则；任何观察尺度优化不得改变同一条鱼的身份、真实状态和关键形体。手机开销必须实测，不承诺全部无限细节零成本；性能不足时报告实际瓶颈，不能私自降成玩具风格。

## 9. 第一轮指令：接管后进入一个真实鱼的结构修正

任务编号：FISH-R1-T01。小妈指定首对象为上述黑鲈；金枪鱼保留为下一项迁移目标。若后来真实用户指令明确改变首对象，记录该指令并遵从，不能仅根据旧助手计划擅自调换。

先完成最少的启动核对：确认分支/封版 SHA、读取本指令与总交接、查 R02 和 Bird、确认 N02 不在候选依赖、定位首参考的实际可读文件。报告一个可识别运行记录和第一条真实执行命令。只有任务或分支，没有执行回执，不算开工。

随后沿现有工作台组织首鱼的固定侧面、正面/头部、斜侧面和必要俯视对照。查清已有原生候选是否存在，不把参考模型渲染当原生候选。若没有候选，只在既定模块边界内建立同一首鱼的结构候选，不建新生态场景。

第一笔生产改动选择整体轮廓/头身比例/头口眼鳃关系中最先阻碍正确性的一个缺陷，真实修改鱼生成函数，并复跑同机位对照；不要同时做全部鱼种、整个材料库和群游。可以只完成一个结构问题，但必须正面显示其他未解决缺陷，不能说首鱼已达到 3A。

先形体和解剖关系，再材质与薄鳍，随后自然动作与生境/游戏接入；这表示首样本的验收顺序，不要求丢弃已有正确的动作测试或停止其他 Mother 的独立工作。进入下一视觉阶段需要小妈确认现阶段证据；不必为每个局部代码修改等待用户再喊“开始”。

若封版或原参考关键输入尚缺，只停止被该缺项影响的生产动作：保存精确缺项和已经做过的检索，完成现有源码/基线/回退检查；不要假造参考，不写另一套通用鱼，不重复要求用户重述所有指示。

## 10. 成果与失败怎么验收

必须分别记录：代码已执行、数值测试、浏览器启动、参考忠实度、材料/动作审查、用户视觉接受、实体设备实测、游戏接入、公开交付。任一通过不能代替其他通过。

结构验收：固定相机、同尺度、同光照下对照轮廓及头口眼鳃鳍位置；指出最大可见误差与不可观测部分。源比例没有米制标定时用明确的归一化比例，不编造绝对尺寸。

工程验收：有限值/有效域、稳定 ID、必要的体段长度与锚点不漂移、无裂缝和穿插、动作相位/暂停/恢复、适用的水体与障碍接触。测试数量不是质量指标，门槛不能为了过关被放宽；原正式门槛的含义需保持，不能把正常每帧移动误算为瞬移。

视觉验收：执行端亲自打开真实渲染截图/页面检查，不只读“截图已保存”。形体错就继续修或明确失败，不叠加特效。实际工作台支持看清整条鱼和近景检查，镜头不能自行漂走；桌面与 390×844 都查。手机尺寸浏览器不等于实体 iPhone。

新候选仅在获准发布、实际部署、版本匹配、入口与依赖 HTTP 200、真实浏览器交互与内部视觉复核通过后，才给一条直接进入工作台的固定在线地址供用户验收。用户接受之前不替换 R02。不得用下载 HTML、ZIP、localhost、生成图片或旧版链接冒充新版；用户明确索要源码/交接文件时另行提供。

内部证据文件足够即可，不再造一整套形式化文件体系：一份运行/变化回执，加代码、真实测试和必要固定视图即可。写清 base/head、命令、结果、限制和下一个单一问题。

## 11. 防止反复思考、过度派工与虚报

在当前任务内允许有目的的局部分析，要求用实验或源码结果结束；禁止无限重写方案、临时改变物种、开启泛化学习或用长篇解释代替实现。

45/90/120 分钟是有实际启动回执后的诊断检查点，分别检查可执行增量、测试/回执及视觉证据或有效阻塞；不是任何实验必须两小时成功的承诺。排队/环境准备、真实工作时间和等待外部输入分开。两小时没有工件也无可复现阻塞，由小妈决定缩小任务或换执行端；不能靠改状态假装平台已取消旧任务。

同一方法失败后下一次必须改变可验证假设，保留反例。两个执行者遇到相同阻塞应升级给小妈处理公共输入/接口问题，不无限换人重试。未知或负结果是有效信息，但不能冒充已满足用户目标。

反馈只需说明：改了哪一处、实际执行证据、参考对照结果、还缺什么、请求小妈决定哪一件事。“有信心”“在努力”“正在思考”不作为成果。没有跨会话工具就把报告写入可访问协调入口，不假称小妈已回复；没有定时执行任务就不声称离线继续工作。

## 12. 新窗口应返回的第一次确认

不用复述长计划。实际读取并运行后返回：

“我是 Fish 执行端；已读取指令 XM-FISH-EXEC-R1-20260919 和封版 SHA …；本轮唯一对象/缺陷为 …；实际命令 … 的结果 …；R02/Bird 保留，N02 未进入依赖；当前已修改 …／确实被 … 阻塞；下一项交付是 …。”

不能填造空白，未运行就写未运行。完成接管本身不等于完成新鱼；接管核对不要变成数轮重复文档工作。

## 来源与本文件边界

本次接续读取的四份旧端文档位于：
`https://github.com/haihao0307/guilin-dem-pipeline/tree/08a861b557fe51fb1c864d6d7a06bb5ecc9179bf/apps/ocean-life-mother/handoff-clean-20260919`

回退事实入口位于：
`https://github.com/haihao0307/guilin-dem-pipeline/blob/9fc023c7eee2245eeb923925f42c7d42e35a02b0/apps/ocean-life-mother/ROLLBACK_R02_20260919.md`

总协调入口：`https://github.com/haihao0307/guilin-dem-pipeline/issues/91`。

本轮工作是用户明确要求的指令/交接文件，不是生产场景改动；未宣称新 Fish 执行会话已经启动。旧端索引中的自然资料与生物参数属于既有研究线索，本文件未重新完成每项外部事实验证。

交付门禁记录：
- [x] 未使用生成图片代替真实三维实现。
- [ ] 本指令编写轮未修改鱼类生产源码；不能计为新鱼成果。
- [ ] 新候选三维工作台、实时画面、公网与真实浏览器待执行端验证。
- [ ] 新鱼用户视觉验收、实体设备验收与 productionReady 均不得擅自置 true。
- [x] 只有截图没有工作台，不能作为鱼类生产交付通过。
