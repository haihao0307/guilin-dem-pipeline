# 日军巡逻艇生产线｜小妈纠错记录｜2026-09-23

本轮明确发生了两项错误：把 ChatGPT 内部 `sandbox:` 地址当成公开网址交付；没有先在真实浏览器中确认船体可见，就宣称三维工作台完成。

从本轮起执行以下强制门槛：

1. 用户收到的必须是公开 HTTPS 地址，不能再用内部 `sandbox:` 地址冒充公开页面。
2. “页面能打开”和“船在画面中真实可见”是两个独立验收项，缺一不可。
3. 每次发布后必须用真实浏览器打开公开入口，检查模型显示和交互，再交给用户。
4. 必须维持一个固定、单一、直接点击的入口，不要求用户下载、解压或启动本地服务器。
