import {chromium} from 'playwright';

const base=process.env.BASE_URL||'http://127.0.0.1:8765/site/dist';
const browser=await chromium.launch({headless:true});
const mobileOptions={viewport:{width:390,height:844},deviceScaleFactor:3,isMobile:true,hasTouch:true,userAgent:'Mozilla/5.0 (iPhone; CPU iPhone OS 18_1 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/18.1 Mobile/15E148 Safari/604.1'};
const failures=[];function assert(v,m){if(!v)failures.push(m);}
const now=()=>performance.now();
async function waitTerrain(page,id){await page.waitForFunction(expected=>{const c=document.querySelector('#terrain');return c?.dataset.ready==='true'&&c?.dataset.patch===expected;},id,{timeout:180000});}
async function waitOsm(page,id){await page.waitForFunction(expected=>{const c=document.querySelector('#terrain');return c?.dataset.osmLoaded==='true'&&c?.dataset.osmPatch===expected;},id,{timeout:180000});}
async function runtimeState(page){return page.evaluate(()=>{const c=document.querySelector('#terrain');return{patch:c?.dataset.patch,osmPatch:c?.dataset.osmPatch,runtime:c?.dataset.osmRuntime||'r3.5-nonindexed',roads:Number(c?.dataset.osmRoadSegmentsDrawn||0),roadRejected:Number(c?.dataset.osmRoadSegmentsRejectedNoSurface||0),buildings:Number(c?.dataset.osmBuildingSegmentsDrawn||0),buildingRejected:Number(c?.dataset.osmBuildingSegmentsRejectedNoSurface||0),roadSourceVertices:Number(c?.dataset.osmRoadSourceVertices||0),roadGpuVertices:Number(c?.dataset.osmRoadGpuVertices||0),roadIndexCount:Number(c?.dataset.osmRoadGpuIndexCount||0),buildMs:Number(c?.dataset.osmBuildMs||0),maxChunkMs:Number(c?.dataset.osmMaxChunkMs||0),yieldCount:Number(c?.dataset.osmYieldCount||0),abortCount:Number(c?.dataset.osmAbortCount||0),fetchAbortCount:Number(c?.dataset.osmFetchAbortCount||0),cacheHits:Number(c?.dataset.osmCacheHits||0),cacheMisses:Number(c?.dataset.osmCacheMisses||0),sourceSha:c?.dataset.osmSourceReleaseSha256,reportSha:c?.dataset.osmCorrectedReportSha256,closure:c?.dataset.osmBuildingSyntheticPatchClosure};});}
async function metrics(page){const cdp=await page.context().newCDPSession(page);await cdp.send('Performance.enable');const raw=await cdp.send('Performance.getMetrics');const m=Object.fromEntries(raw.metrics.map(x=>[x.name,x.value]));const dom=await cdp.send('Memory.getDOMCounters');await cdp.detach();return{heapUsed:m.JSHeapUsedSize,heapTotal:m.JSHeapTotalSize,taskDuration:m.TaskDuration,scriptDuration:m.ScriptDuration,nodes:dom.nodes,documents:dom.documents,eventListeners:dom.jsEventListeners};}
async function rafProbe(page,n=30){return page.evaluate(frames=>new Promise(resolve=>{const a=[];let last=performance.now(),i=0;function tick(t){a.push(t-last);last=t;if(++i>=frames){const s=[...a].sort((x,y)=>x-y),p=q=>s[Math.min(s.length-1,Math.floor((s.length-1)*q))];resolve({frames,meanMs:a.reduce((x,y)=>x+y,0)/a.length,p50Ms:p(.5),p95Ms:p(.95),maxMs:Math.max(...a),over50:a.filter(x=>x>50).length});}else requestAnimationFrame(tick);}requestAnimationFrame(tick);}),n);}
async function selectTimed(page,id){const t0=now();await page.selectOption('#location',id);await waitTerrain(page,id);const terrainReadyMs=now()-t0;await waitOsm(page,id);return{terrainReadyMs,osmReadyMs:now()-t0,state:await runtimeState(page)};}
async function runOne(version,label){
  const context=await browser.newContext(mobileOptions),page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(`pageerror:${e.message}`));page.on('console',m=>{if(m.type()==='error')errors.push(`console:${m.text()}`);});
  const nav0=now();await page.goto(`${base}/${version}/`,{waitUntil:'domcontentloaded',timeout:180000});await page.waitForFunction(()=>document.querySelector('#terrain')?.dataset.ready==='true',null,{timeout:180000});const initialTerrainReadyMs=now()-nav0;
  const q=await selectTimed(page,'query-01');const qMetrics=await metrics(page),qRaf=await rafProbe(page,30);
  const o=await selectTimed(page,'overview');const oMetrics=await metrics(page),oRaf=await rafProbe(page,30);
  assert(q.state.roads===11007&&q.state.roadRejected===109&&q.state.buildings===906&&q.state.buildingRejected===4,`${label} q01 geometry ${JSON.stringify(q.state)}`);
  assert(o.state.roads===508201&&o.state.roadRejected===1411&&o.state.buildings===0,`${label} overview geometry ${JSON.stringify(o.state)}`);
  assert(q.state.sourceSha==='f3ae1c9051b25fc1d23c8df1dd951a9138d6b188b1e46bff56a352c324a90101'&&q.state.reportSha==='d9a2d7986f74cfd4309174151dbc36920e188080f4c1daf21ae865efa3dd4364',`${label} evidence SHA changed`);
  assert(q.state.closure==='false',`${label} synthetic closure changed`);
  if(version==='r3-6')assert(q.state.runtime==='indexed-r36'&&q.state.roadSourceVertices===q.state.roadGpuVertices&&q.state.roadIndexCount===q.state.roads*2,`${label} indexed runtime mismatch ${JSON.stringify(q.state)}`);
  await context.close();return{label,version,initialTerrainReadyMs,query01:{...q,metrics:qMetrics,raf:qRaf},overview:{...o,metrics:oMetrics,raf:oRaf},errors};
}
async function cancellationStress(version){
  const context=await browser.newContext(mobileOptions);let started=0,finished=0,failed=0;const failedDetails=[];const match=u=>u.includes('/site/dist/r3-5/data/osm/')&&(u.endsWith('.u16le')||u.endsWith('.u32le'));
  context.on('request',r=>{if(match(r.url()))started++;});context.on('requestfinished',r=>{if(match(r.url()))finished++;});context.on('requestfailed',r=>{if(match(r.url())){failed++;failedDetails.push({url:r.url(),failure:r.failure()?.errorText});}});
  await context.route('**/site/dist/r3-5/data/osm/*',async route=>{await new Promise(r=>setTimeout(r,350));try{await route.continue();}catch{}});
  const page=await context.newPage();await page.goto(`${base}/${version}/`,{waitUntil:'domcontentloaded',timeout:180000});await page.waitForFunction(()=>document.querySelector('#terrain')?.dataset.ready==='true',null,{timeout:180000});
  for(const id of['query-01','query-02','query-03']){await page.selectOption('#location',id);await waitTerrain(page,id);}await waitOsm(page,'query-03');await page.waitForTimeout(1200);const state=await runtimeState(page);await context.close();return{version,delayMs:350,started,finished,failed,failedDetails,state};
}
const runs=[];for(const [v,l] of[['r3-5','A1-r3.5'],['r3-6','B1-r3.6'],['r3-6','B2-r3.6'],['r3-5','A2-r3.5']])runs.push(await runOne(v,l));
const cancellation=[await cancellationStress('r3-5'),await cancellationStress('r3-6')];
function avg(vals){return vals.reduce((a,b)=>a+b,0)/vals.length;}function pair(path){const pick=(r,p)=>p.reduce((v,k)=>v[k],r);const a=runs.filter(r=>r.version==='r3-5').map(r=>pick(r,path)),b=runs.filter(r=>r.version==='r3-6').map(r=>pick(r,path));const av=avg(a),bv=avg(b);return{r35:a,r36:b,r35Mean:av,r36Mean:bv,r36OverR35:bv/av,percentChange:(bv/av-1)*100};}
const summary={queryOsmReady:pair(['query01','osmReadyMs']),overviewOsmReady:pair(['overview','osmReadyMs']),queryHeap:pair(['query01','metrics','heapUsed']),overviewHeap:pair(['overview','metrics','heapUsed']),queryRafP50:pair(['query01','raf','p50Ms']),overviewRafP50:pair(['overview','raf','p50Ms'])};
assert(runs.every(r=>r.errors.length===0),`runtime errors ${JSON.stringify(runs.map(r=>r.errors))}`);assert(cancellation[0].failed===0,'R3.5 cancellation baseline unexpectedly failed payload requests');assert(cancellation[1].state.runtime==='indexed-r36','R3.6 cancellation state missing runtime marker');
const report={schema:'wenzhou-r3.6-paired-ab/r1',environment:'same GitHub Actions runner, same Chromium process, fresh contexts; mobile emulation only, NOT real iPhone/Safari/GPU',order:runs.map(r=>r.label),runs,cancellation,summary,realIphoneVerified:false,failures};console.log(JSON.stringify(report,null,2));await browser.close();if(failures.length)process.exit(1);
