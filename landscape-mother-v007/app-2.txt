f(uDiag==2)color=vec3(run*.15,run*.62,run);else if(uDiag==3)color=vec3(age,.48*age,.16);else if(uDiag==4)color=vec3(conc);else if(uDiag==5)color=mix(vec3(.12,.7,.18),vec3(.92,.26,.12),rock);color=vec3(1.0)-exp(-color*uExposure);gl_FragColor=vec4(l2s(color),1.0);}`;
const BVS1=`attribute vec2 aPosition;varying vec2 vUV;void main(){vUV=aPosition*.5+.5;gl_Position=vec4(aPosition,0.0,1.0);}`;
const BFS1=`precision mediump float;varying vec2 vUV;uniform int uLight;void main(){vec3 top=vec3(.39,.64,.76),bottom=vec3(.76,.82,.78);if(uLight==1){top=vec3(.30,.47,.66);bottom=vec3(.90,.69,.44);}else if(uLight==2){top=vec3(.61,.68,.72);bottom=vec3(.77,.78,.75);}else if(uLight==3){top=vec3(.35,.61,.72);bottom=vec3(.69,.79,.77);}vec3 c=mix(bottom,top,smoothstep(0.0,1.0,vUV.y));gl_FragColor=vec4(c,1.0);}`;
function initGL(){let opts={antialias:true,alpha:false,depth:true,stencil:false,preserveDrawingBuffer:false,powerPreference:mobile?'default':'high-performance'};gl=canvas.getContext('webgl2',opts);isGL2=!!gl;if(!gl)gl=canvas.getContext('webgl',opts)||canvas.getContext('experimental-webgl',opts);if(!gl)throw Error('当前浏览器没有开启 WebGL');let vs=VS1,fs=FS1,bvs=BVS1,bfs=BFS1;if(isGL2){vs='#version 300 es\n'+VS1.replace(/attribute/g,'in').replace(/varying vec3 vWorld,vNormal;/,'out vec3 vWorld,vNormal;').replace(/varying vec4 vField;/,'out vec4 vField;');fs='#version 300 es\n'+FS1.replace(/varying vec3 vWorld,vNormal;/,'in vec3 vWorld,vNormal;').replace(/varying vec4 vField;/,'in vec4 vField;').replace(/void main\(\)\{/,'out vec4 outColor;void main(){').replace(/gl_FragColor/g,'outColor');bvs='#version 300 es\n'+BVS1.replace(/attribute/g,'in').replace(/varying vec2 vUV;/,'out vec2 vUV;');bfs='#version 300 es\n'+BFS1.replace(/varying vec2 vUV;/,'in vec2 vUV;').replace(/void main\(\)\{/,'out vec4 outColor;void main(){').replace(/gl_FragColor/g,'outColor')}program=makeProgram(vs,fs);bgProgram=makeProgram(bvs,bfs);buffers.pos=gl.createBuffer();buffers.norm=gl.createBuffer();buffers.field=gl.createBuffer();buffers.idx=gl.createBuffer();buffers.bg=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffers.bg);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.enable(gl.CULL_FACE);gl.cullFace(gl.BACK)}
function attr(p,name,size,buffer){let loc=gl.getAttribLocation(p,name);if(loc<0)return;gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,size,gl.FLOAT,false,0,0)}
async function buildGeometry(initial=false){let token=++buildToken,R=recipe(S.seed),count=N*N,H=new Float32Array(count),maxH=0,rows=mobile?10:14;rebuild.classList.add('show');rebuildText.textContent=initial?'正在建立喀斯特函数场':'正在更新地貌种子';for(let k=0;k<N;k++){for(let i=0;i<N;i++){let h=heightAt(X0+i*DX,Z0+k*DZ,R,S.seed);H[k*N+i]=h;if(h>maxH)maxH=h}if(k%rows===0){if(token!==buildToken)return;rebuildText.textContent='函数场 '+Math.round(48*k/(N-1))+'%';await tick()}}let P=new Float32Array(count*3),Q=new Float32Array(count*3),F=new Float32Array(count*4),rockCells=0,steep=0;for(let k=0;k<N;k++){for(let i=0;i<N;i++){let q=k*N+i,h=H[q],il=Math.max(0,i-1),ir=Math.min(N-1,i+1),kd=Math.max(0,k-1),ku=Math.min(N-1,k+1),hl=H[k*N+il],hr=H[k*N+ir],hd=H[kd*N+i],hu=H[ku*N+i],gx=(hr-hl)/(Math.max(1,ir-il)*DX),gz=(hu-hd)/(Math.max(1,ku-kd)*DZ),nx=-gx,ny=1,nz=-gz,nl=Math.hypot(nx,ny,nz);nx/=nl;ny/=nl;nz/=nl;let slope=1-Math.abs(ny),lap=(hl+hr+hd+hu)*.25-h,conc=clamp(.5+lap*1.52),low=1-clamp(h/(maxH*.67)),runoff=clamp(.20+.50*conc+.24*low+.12*fbm((X0+i*DX)*.42,(Z0+k*DZ)*.42,S.seed+700,2)),age=clamp(.52+.34*fbm((