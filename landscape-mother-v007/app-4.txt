uni(program,'uProj'),false,perspective(Math.PI/4.15,canvas.width/canvas.height,.08,120));gl.uniform3f(uni(program,'uCamera'),C.eye[0],C.eye[1],C.eye[2]);let a=2.32,e=.83,L=[Math.cos(a)*Math.cos(e),Math.sin(e),Math.sin(a)*Math.cos(e)],sc=[1,.92,.76],fc=[.40,.58,.72],fd=[-.58,.50,-.63];if(S.light==='golden'){sc=[1,.69,.40];fc=[.31,.49,.66]}else if(S.light==='neutral'){sc=[1,1,1];fc=[.66,.68,.69];fd=[-.40,.60,-.50]}else if(S.light==='wet'){sc=[.91,.97,1];fc=[.37,.60,.70]}gl.uniform3f(uni(program,'uSunDir'),L[0],L[1],L[2]);gl.uniform3f(uni(program,'uSunColor'),sc[0],sc[1],sc[2]);gl.uniform3f(uni(program,'uFillDir'),fd[0],fd[1],fd[2]);gl.uniform3f(uni(program,'uFillColor'),fc[0],fc[1],fc[2]);for(const [n,v] of [['uSun',S.sun],['uAmbient',S.ambient],['uExposure',S.exposure],['uCalcite',S.calcite],['uOchre',S.ochre],['uMoss',S.moss],['uWetness',S.wetness+(S.light==='wet'?.18:0)],['uMicro',S.micro]])gl.uniform1f(uni(program,n),v);gl.uniform1i(uni(program,'uDiag'),S.diag);gl.drawElements(gl.TRIANGLES,indexCount,gl.UNSIGNED_SHORT,0);window.__LANDSCAPE_GL_ERROR__=gl.getError()}
function draw(){if(!raf)raf=requestAnimationFrame(render)}
function bind(id,key,kind='render'){let e=$(id),o=$(id+'V');S[key]=+e.value;o.textContent=S[key].toFixed(2);e.oninput=()=>{S[key]=+e.value;o.textContent=S[key].toFixed(2);if(kind==='geometry'){clearTimeout(bind.g);bind.g=setTimeout(()=>buildGeometry(false),140)}else draw()}}
function setSeed(n){S.seed=clamp(Math.floor(+n||28117),1,999999);buildGeometry(false)}
function bindUI(){bind('hardness','hardness','geometry');bind('relief','relief','geometry');bind('grooves','grooves','geometry');bind('sinkholes','sinkholes','geometry');bind('calcite','calcite');bind('ochre','ochre');bind('moss','moss');bind('wetness','wetness');bind('micro','micro');bind('sun','sun');bind('ambient','ambient');bind('exposure','exposure');prevSeed.onclick=()=>setSeed(S.seed-1);nextSeed.onclick=()=>setSeed(S.seed+1);randomSeed.onclick=()=>setSeed(1+Math.floor(Math.random()*999998));seedInput.onchange=e=>setSeed(e.target.value);curated.forEach(n=>{let b=document.createElement('button');b.textContent=n;b.dataset.seed=n;b.onclick=()=>setSeed(n);seedChips.appendChild(b)});document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('.page').forEach(x=>x.classList.toggle('active',x.id===b.dataset.tab))});document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));document.querySelectorAll('[data-light]').forEach(b=>b.onclick=()=>{S.light=b.dataset.light;document.querySelectorAll('[data-light]').forEach(x=>x.classList.toggle('active',x===b));draw()});document.querySelectorAll('[data-diag]').forEach(b=>b.onclick=()=>{S.diag=+b.dataset.diag;document.querySelectorAll('[data-diag]').forEach(x=>x.classList.toggle('active',x===b));draw()});panelBtn.onclick=()=>panel.classList.toggle('hidden');customBtn.onclick=()=>{panel.classList.remove('hidden');document.querySelector('[data-tab="light"]').click()};retry.onclick=()=>location.reload()}
const pointers=new Map();let lastSingle=null,lastPinch=0,lastMid=null;canvas.addEventListener('pointerdown',e=>{canvas.setPointerCapture(e.pointerId);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===1)lastSingle={x:e.clientX,y:e.clientY};if(pointers.size===2){let a=[...pointers.values()];lastPinch=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y);lastMid={x:(a[0].x+a[1].x)/2,y:(a[0].y+a[1].y)/2}}});canvas.addEventListener('pointermove',e=>{if(!pointers.has(e.p