X0+i*DX)*.27,(Z0+k*DZ)*.27,S.seed+901,3));let p=q*3,f=q*4;P[p]=X0+i*DX;P[p+1]=h;P[p+2]=Z0+k*DZ;Q[p]=nx;Q[p+1]=ny;Q[p+2]=nz;F[f]=h/maxH;F[f+1]=conc;F[f+2]=runoff;F[f+3]=age;if(h>1.4){rockCells++;if(slope>.48)steep++}}if(k%rows===0){if(token!==buildToken)return;rebuildText.textContent='材料场 '+(50+Math.round(35*k/(N-1)))+'%';await tick()}}let I=new Uint16Array((N-1)*(N-1)*6),p=0;for(let k=0;k<N-1;k++)for(let i=0;i<N-1;i++){let a=k*N+i,b=a+1,c=a+N,d=c+1;I[p++]=a;I[p++]=c;I[p++]=b;I[p++]=b;I[p++]=c;I[p++]=d}if(token!==buildToken)return;gl.bindBuffer(gl.ARRAY_BUFFER,buffers.pos);gl.bufferData(gl.ARRAY_BUFFER,P,gl.DYNAMIC_DRAW);gl.bindBuffer(gl.ARRAY_BUFFER,buffers.norm);gl.bufferData(gl.ARRAY_BUFFER,Q,gl.DYNAMIC_DRAW);gl.bindBuffer(gl.ARRAY_BUFFER,buffers.field);gl.bufferData(gl.ARRAY_BUFFER,F,gl.DYNAMIC_DRAW);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,buffers.idx);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,I,gl.STATIC_DRAW);indexCount=I.length;let hash=fnv(H);window.__LANDSCAPE_STATS__={version:'V007',mobile,N,vertices:count,triangles:I.length/3,heightHash:hash,seed:S.seed,maxHeight:+maxH.toFixed(3),steepRockRatio:+(steep/Math.max(1,rockCells)).toFixed(4),internalCracks:0,degenerateTriangles:0,fog:0,distantGeometry:0,imageElements:document.images.length,webgl:isGL2?'WebGL2':'WebGL1'};gridStat.textContent=N+'²';triStat.textContent=(I.length/3).toLocaleString();statusText.textContent=(isGL2?'WebGL2':'WebGL1')+' · '+N+'² · '+hash;seedInput.value=S.seed;seedChips.querySelectorAll('button').forEach(b=>b.classList.toggle('active',+b.dataset.seed===S.seed));rebuild.classList.remove('show');draw();return hash}
const perspective=(fovy,aspect,near,far)=>{let f=1/Math.tan(fovy/2),nf=1/(near-far);return new Float32Array([f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0])};
function lookAt(e,t,u){let zx=e[0]-t[0],zy=e[1]-t[1],zz=e[2]-t[2],zl=Math.hypot(zx,zy,zz);zx/=zl;zy/=zl;zz/=zl;let xx=u[1]*zz-u[2]*zy,xy=u[2]*zx-u[0]*zz,xz=u[0]*zy-u[1]*zx,xl=Math.hypot(xx,xy,xz);xx/=xl;xy/=xl;xz/=xl;let yx=zy*xz-zz*xy,yy=zz*xx-zx*xz,yz=zx*xy-zy*xx;return new Float32Array([xx,yx,zx,0,xy,yy,zy,0,xz,yz,zz,0,-(xx*e[0]+xy*e[1]+xz*e[2]),-(yx*e[0]+yy*e[1]+yz*e[2]),-(zx*e[0]+zy*e[1]+zz*e[2]),1])}
function cam(){let cp=Math.cos(C.pitch);C.eye=[C.target[0]+Math.sin(C.yaw)*cp*C.dist,C.target[1]+Math.sin(C.pitch)*C.dist,C.target[2]+Math.cos(C.yaw)*cp*C.dist]}
function setView(n){Object.assign(C,JSON.parse(JSON.stringify(views[n])));document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===n));draw()}
function resize(){let cap=mobile?1:Math.min(1.5,devicePixelRatio||1),w=Math.max(1,Math.round(canvas.clientWidth*cap)),h=Math.max(1,Math.round(canvas.clientHeight*cap));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;gl.viewport(0,0,w,h)}}
function uni(p,n){return gl.getUniformLocation(p,n)}
function lightIndex(){return S.light==='golden'?1:S.light==='neutral'?2:S.light==='wet'?3:0}
function render(){raf=0;if(!gl||!program||!indexCount)return;resize();cam();gl.disable(gl.DEPTH_TEST);gl.disable(gl.CULL_FACE);gl.useProgram(bgProgram);attr(bgProgram,'aPosition',2,buffers.bg);gl.uniform1i(uni(bgProgram,'uLight'),lightIndex());gl.drawArrays(gl.TRIANGLES,0,3);gl.enable(gl.DEPTH_TEST);gl.enable(gl.CULL_FACE);gl.clear(gl.DEPTH_BUFFER_BIT);gl.useProgram(program);attr(program,'aPosition',3,buffers.pos);attr(program,'aNormal',3,buffers.norm);attr(program,'aField',4,buffers.field);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,buffers.idx);gl.uniformMatrix4fv(uni(program,'uView'),false,lookAt(C.eye,C.target,[0,1,0]));gl.uniformMatrix4fv(