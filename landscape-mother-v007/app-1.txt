(async()=>{'use strict';
const $=id=>document.getElementById(id),canvas=$('gl'),loading=$('loading'),loadText=$('loadText'),loadBar=$('loadBar'),loadPct=$('loadPct'),retry=$('retry'),panel=$('panel');
const coarse=matchMedia('(pointer:coarse)').matches||/iPhone|iPad|iPod|Android/i.test(navigator.userAgent),mobile=coarse||innerWidth<720;
let N=mobile?129:193,WX=28,WZ=22,X0=-WX/2,Z0=-WZ/2,DX=WX/(N-1),DZ=WZ/(N-1),gl=null,isGL2=false,program=null,bgProgram=null,vao=null,buffers={},indexCount=0,raf=0,buildToken=0,ready=false;
const curated=[28117,45221,68103,82771,118097,144703,216091,331777];
const S={seed:28117,hardness:1,relief:1.02,grooves:.47,sinkholes:.38,calcite:1.04,ochre:.68,moss:.43,wetness:.21,micro:.50,sun:4.65,ambient:.96,exposure:1.14,diag:0,light:'clear'};
const C={yaw:.74,pitch:.30,dist:28.5,target:[0,5.15,0],eye:[0,0,0]},views={hero:{yaw:.74,pitch:.30,dist:28.5,target:[0,5.15,0]},cliff:{yaw:1.18,pitch:.16,dist:17.2,target:[-2.1,5.9,-.1]},top:{yaw:.35,pitch:.82,dist:24.2,target:[-.5,5.6,-.7]},surface:{yaw:1.02,pitch:.08,dist:11.5,target:[-3.2,5.1,.2]}};
const clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,x)),mix=(a,b,t)=>a+(b-a)*t,smooth=(a,b,x)=>{let t=clamp((x-a)/(b-a));return t*t*(3-2*t)},tick=()=>new Promise(r=>requestAnimationFrame(r));
function progress(p,t){loadBar.style.width=p+'%';loadPct.textContent=Math.round(p)+'%';if(t)loadText.textContent=t}
function hash2(i,k,s){let n=(Math.imul(i,374761393)+Math.imul(k,668265263)+Math.imul(s,1442695041))|0;n^=n>>>13;n=Math.imul(n,1274126177);return((n^(n>>>16))>>>0)/4294967295}
function noise(x,z,s){let i=Math.floor(x),k=Math.floor(z),fx=x-i,fz=z-k,u=fx*fx*(3-2*fx),v=fz*fz*(3-2*fz);return mix(mix(hash2(i,k,s),hash2(i+1,k,s),u),mix(hash2(i,k+1,s),hash2(i+1,k+1,s),u),v)*2-1}
function fbm(x,z,s,o=3){let a=.54,f=1,sum=0,n=0;for(let j=0;j<o;j++){sum+=a*noise(x*f,z*f,s+j*97);n+=a;f*=2.02;a*=.47}return sum/n}
function rng(seed){let a=seed>>>0;return()=>{a|=0;a=a+0x6D2B79F5|0;let t=Math.imul(a^a>>>15,1|a);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296}}
function rot(x,z,a){let c=Math.cos(a),s=Math.sin(a);return[x*c+z*s,-x*s+z*c]}
function segD(x,z,a,b){let vx=b.cx-a.cx,vz=b.cz-a.cz,wx=x-a.cx,wz=z-a.cz,t=clamp((wx*vx+wz*vz)/(vx*vx+vz*vz));return Math.hypot(x-(a.cx+vx*t),z-(a.cz+vz*t))}
function fnv(arr){let h=2166136261>>>0;for(let i=0;i<arr.length;i+=11){h^=Math.round(arr[i]*10000)|0;h=Math.imul(h,16777619)}return(h>>>0).toString(16).padStart(8,'0')}
function recipe(seed){let r=rng(seed),base=[[-3.8,-.3,14.4,3.65,3.0],[3.2,-1.15,12.8,3.1,2.7],[-.05,-4.35,10.9,2.8,2.4],[.8,4.15,8.8,2.85,2.3]];return base.map(p=>({cx:p[0]+(r()-.5)*.62,cz:p[1]+(r()-.5)*.52,h:p[2]*(.93+r()*.14),rx:p[3]*(.94+r()*.12),rz:p[4]*(.94+r()*.12),rot:(r()-.5)*.68,pow:5.2+r()*2.2,phase:r()*6.283,k:5+Math.floor(r()*3),sink:r()*6.283}))}
function tower(x,z,t,seed){let q=rot(x-t.cx,z-t.cz,t.rot),u=q[0],v=q[1],n=fbm(u*.40+2.1,v*.40-1.4,seed+19,3),warp=1+.045*n+.018*Math.sin(Math.atan2(v,u)*3+t.phase),rr=Math.pow(Math.pow(Math.abs(u)/(t.rx*warp),t.pow)+Math.pow(Math.abs(v)/(t.rz*warp),t.pow),1/t.pow),hc=clamp((S.hardness-.76)/.42),inner=mix(.63,.48,hc),body=Math.pow(clamp(1-smooth(inner,1.012,rr)),mix(.50,.27,hc));let h=t.h*S.relief*body;if(h<=.001)return 0;let top=smooth(.34,.86,body),ang=Math.atan2(v,u);h+=top*(.32*n+.13*Math.sin(u*.67+v*.31+t.phase));let side=smooth(.30,.68,rr)*(1-smooth(.88,1.01,rr)),channels=Math.pow(Math.max(0,Math.sin(ang*t.k+t.phase+n*1.4)),12);h-=S.grooves*side*channels*(.58+.92*(1-body));let sx=.22*Math.cos(t.sink),sz=.18*Math.sin(t.sink),crater=Math.exp(-((u/t.rx-sx)**2/.055+(v/t.rz-sz)**2/.07));h-=S.sinkholes*top*crater*(.68+.82*(1-body));let scar=Math.pow(Math.max(0,Math.sin(u*.72-v*.27+t.phase)),22)*smooth(.40,.82,rr)*(1-smooth(.87,.98,rr));return Math.max(0,h-scar*.22*S.grooves)}
function heightAt(x,z,R,seed){let ground=.15+.09*fbm(x*.20,z*.20,seed+301,3)+.026*fbm(x*.78,z*.78,seed+311,2),h=ground;for(let n=0;n<R.length;n++)h=Math.max(h,tower(x,z,R[n],seed));let b1=4.5*S.relief*Math.pow(1-smooth(1.25,3.25,segD(x,z,R[0],R[1])),.76),b2=2.9*S.relief*Math.pow(1-smooth(.9,2.35,segD(x,z,R[0],R[2])),.84);h=Math.max(h,ground+Math.max(0,b1),ground+Math.max(0,b2));let edge=Math.max(Math.abs(x)/(WX*.5),Math.abs(z)/(WZ*.5));return ground+(h-ground)*(1-smooth(.78,.985,edge))}
function shader(type,src){let s=gl.createShader(type);gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(s)||'shader compile failed');return s}
function makeProgram(vs,fs){let p=gl.createProgram();gl.attachShader(p,shader(gl.VERTEX_SHADER,vs));gl.attachShader(p,shader(gl.FRAGMENT_SHADER,fs));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p)||'program link failed');return p}
const VS1=`precision highp float;attribute vec3 aPosition;attribute vec3 aNormal;attribute vec4 aField;uniform mat4 uView,uProj;varying vec3 vWorld,vNormal;varying vec4 vField;void main(){vWorld=aPosition;vNormal=aNormal;vField=aField;gl_Position=uProj*uView*vec4(aPosition,1.0);}`;
const FS1=`precision highp float;varying vec3 vWorld,vNormal;varying vec4 vField;uniform vec3 uCamera,uSunDir,uSunColor,uFillDir,uFillColor;uniform float uSun,uAmbient,uExposure,uCalcite,uOchre,uMoss,uWetness,uMicro;uniform int uDiag;float sat(float x){return clamp(x,0.0,1.0);}vec3 s2l(vec3 c){return pow(max(c,0.0),vec3(2.2));}vec3 l2s(vec3 c){return pow(max(c,0.0),vec3(1.0/2.2));}float pattern(vec3 p){return .5+.5*sin(p.x*2.7+sin(p.z*1.9)+p.y*.74)*sin(p.z*3.1-p.y*.43); }float fine(vec3 p){return .5+.5*sin(dot(p,vec3(8.3,5.7,7.1))+sin(dot(p,vec3(3.2,6.4,2.8))));}void main(){vec3 N=normalize(vNormal),V=normalize(uCamera-vWorld),L=normalize(uSunDir),F=normalize(uFillDir);float slope=1.0-abs(N.y),h=vField.x,conc=vField.y,run=vField.z,age=vField.w,grain=mix(pattern(vWorld*.44),fine(vWorld*.19),.35);float rock=smoothstep(.055,.16,h)+smoothstep(.28,.72,slope);rock=sat(rock);float fresh=sat((1.0-age)*.75+grain*.18+slope*.12),och=sat(uOchre*(conc*.53+age*.30+grain*.15)*rock),moss=sat(uMoss*(1.0-smoothstep(.24,.78,slope))*conc*(1.0-h*.55)),wet=sat((uWetness+run*.42)*run*(.42+slope*.58));vec3 calcite=vec3(.61,.575,.49)*uCalcite,freshC=vec3(.79,.735,.60),ochreC=vec3(.50,.31,.13),mossC=vec3(.17,.285,.125),wetC=vec3(.20,.245,.225),soilC=vec3(.29,.19,.105),grassC=vec3(.22,.36,.16);vec3 rockC=mix(calcite,freshC,fresh*.42);rockC=mix(rockC,ochreC,och*.56);rockC=mix(rockC,mossC,moss*.66);rockC=mix(rockC,wetC,wet*.58);vec3 groundC=mix(soilC,grassC,sat(.28+conc*.58+grain*.20));vec3 base=mix(groundC,rockC,rock);float rough=clamp(mix(.82,.69,rock)-wet*.27+uMicro*(grain-.5)*.16,.34,.94);float ao=clamp(1.0-conc*.27-rock*wet*.06,.58,1.0);float ndl=max(dot(N,L),0.0),ndf=max(dot(N,F),0.0);vec3 H=normalize(L+V);float nh=max(dot(N,H),0.0),vh=max(dot(V,H),0.0),specPow=mix(72.0,9.0,rough),specShape=pow(nh,specPow)*(specPow+2.0)/8.0;vec3 F0=vec3(.04),fres=F0+(1.0-F0)*pow(1.0-vh,5.0);vec3 baseLin=s2l(clamp(base,0.0,1.0));vec3 diffuse=baseLin*(uAmbient*ao+uSun*ndl*uSunColor+1.05*ndf*uFillColor);vec3 spec=fres*specShape*(uSun*ndl*.42+ndf*.16)*(1.0-wet*.08);vec3 color=diffuse+spec;if(uDiag==1)color=vec3(slope);else i