ointerId))return;pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});let a=[...pointers.values()];if(a.length===1&&lastSingle){let dx=e.clientX-lastSingle.x,dy=e.clientY-lastSingle.y;C.yaw-=dx*.006;C.pitch=clamp(C.pitch-dy*.005,-.02,1.18);lastSingle={x:e.clientX,y:e.clientY};draw()}else if(a.length>=2){let d=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y),m={x:(a[0].x+a[1].x)/2,y:(a[0].y+a[1].y)/2};if(lastPinch>0)C.dist=clamp(C.dist*lastPinch/Math.max(1,d),7.2,52);if(lastMid){let dx=m.x-lastMid.x,dy=m.y-lastMid.y,right=[Math.cos(C.yaw),0,-Math.sin(C.yaw)],s=C.dist*.0014;C.target[0]-=right[0]*dx*s;C.target[2]-=right[2]*dx*s;C.target[1]+=dy*s}lastPinch=d;lastMid=m;draw()}});function endPointer(e){pointers.delete(e.pointerId);lastSingle=pointers.size===1?{...pointers.values().next().value}:null;if(pointers.size<2){lastPinch=0;lastMid=null}}canvas.addEventListener('pointerup',endPointer);canvas.addEventListener('pointercancel',endPointer);canvas.addEventListener('wheel',e=>{e.preventDefault();C.dist=clamp(C.dist*Math.exp(e.deltaY*.001),7.2,52);draw()},{passive:false});canvas.oncontextmenu=e=>e.preventDefault();window.addEventListener('resize',draw);
window.__setLandscapeSeed__=setSeed;window.__setLandscapeView__=setView;window.__requestLandscapeMetrics__=()=>({ready,stats:window.__LANDSCAPE_STATS__,glError:gl?gl.getError():-1,canvas:[canvas.width,canvas.height],css:[canvas.clientWidth,canvas.clientHeight],loading:loading.className,mobile,images:document.images.length,externalAssets:0,fog:0,distantGeometry:0});window.__requestLandscapeFrameMetrics__=()=>{let old=S.diag;resize();S.diag=5;render();let w=canvas.width,h=canvas.height,mask=new Uint8Array(w*h*4);gl.readPixels(0,0,w,h,gl.RGBA,gl.UNSIGNED_BYTE,mask);S.diag=0;render();let px=new Uint8Array(w*h*4);gl.readPixels(0,0,w,h,gl.RGBA,gl.UNSIGNED_BYTE,px);let count=0,lum=0,chr=0,colorful=0,dark=0,bright=0,step=mobile?16:24;for(let i=0;i<px.length;i+=step){let rM=mask[i],gM=mask[i+1];if(!(rM>gM*1.18&&rM>95))continue;let r=px[i]/255,g=px[i+1]/255,b=px[i+2]/255,mx=Math.max(r,g,b),mn=Math.min(r,g,b),c=mx-mn,l=.2126*r+.7152*g+.0722*b;count++;lum+=l;chr+=c;colorful+=c>.055?1:0;dark+=l<.12?1:0;bright+=l>.90?1:0}S.diag=old;render();let q=Math.max(1,count);return{rockSamples:count,meanLuminance:lum/q,meanChroma:chr/q,colorfulRatio:colorful/q,darkRatio:dark/q,brightRatio:bright/q,glError:gl.getError(),seed:S.seed,heightHash:window.__LANDSCAPE_STATS__&&window.__LANDSCAPE_STATS__.heightHash}};
try{progress(4,'正在启动移动端三维内核');await tick();initGL();progress(18,(isGL2?'WebGL2':'WebGL1')+' 图形内核就绪');await tick();bindUI();progress(26,'正在建立连续共享网格');await tick();let hash=await buildGeometry(true);progress(90,'正在绘制清晰彩色岩面');await tick();setView('hero');render();await tick();progress(100,'三维地貌已完成');ready=true;window.__LANDSCAPE_READY__=true;setTimeout(()=>{loading.classList.add('done');setTimeout(()=>loading.style.display='none',360)},120);if(mobile)panel.classList.add('hidden')}catch(err){console.error(err);ready=false;window.__LANDSCAPE_READY__=false;loading.classList.add('error');loadText.textContent='三维内核启动失败：'+String(err.message||err);loadPct.textContent='请点“重新启动”';loadBar.style.width='100%'}
})();