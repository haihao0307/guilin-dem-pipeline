import * as THREE from 'three';const S=window.R40,{mobile,scene,MAT,mesh}=S;const W=window.W,{clamp,mix,sm,hash,noise,ridged,rng,geomDetail,fineDetail,mountains,mountainField,baseNatural,terraceMask,terraceInfoFromNatural,nodes,flatFields,flatFieldAt,riverZ,sourcePath,channelSpecs,channelSurface,terrainY}=W;
const EXT={xmin:-220,xmax:220,zmin:-290,zmax:185,step:mobile?2.2:1.75},sx=Math.round((EXT.xmax-EXT.xmin)/EXT.step),sz=Math.round((EXT.zmax-EXT.zmin)/EXT.step),tg=new THREE.PlaneGeometry(EXT.xmax-EXT.xmin,EXT.zmax-EXT.zmin,sx,sz);tg.rotateX(-Math.PI/2);tg.translate((EXT.xmin+EXT.xmax)/2,0,(EXT.zmin+EXT.zmax)/2);const tp=tg.attributes.position,vc=new Float32Array(tp.count*3),C=new THREE.Color();for(let i=0;i<tp.count;i++){const x=tp.getX(i),z=tp.getZ(i),y=terrainY(x,z);tp.setY(i,y);const nat=baseNatural(x,z),tm=terraceMask(x,z),ff=flatFieldAt(x,z),fine=fineDetail(x,z);if(z>125&&Math.abs(z-riverZ(x))<14)C.set('#496b54');else if(ff)C.set(['#667847','#6b8144','#758542','#897f43'][ff.stage]);else if(tm>.45){const ti=terraceInfoFromNatural(nat,x,z);C.set(ti.f>.76?'#76573a':(ti.k%4===3?'#80763f':ti.k%3===0?'#61803f':'#6d8543'));}else if(nat>34)C.set('#354f40');else if(nat>18)C.set('#3f6a3e');else C.set('#5f8247');C.multiplyScalar(.92+fine*.055);vc[i*3]=C.r;vc[i*3+1]=C.g;vc[i*3+2]=C.b;}tg.setAttribute('color',new THREE.BufferAttribute(vc,3));tg.computeVertexNormals();const terrain=mesh(tg,MAT.terrain);terrain.receiveShadow=true;
function ribbon2D(points,width,mat,offset=.05){const pos=[],ix=[];for(let i=0;i<points.length;i++){const a=points[Math.max(0,i-1)],b=points[Math.min(points.length-1,i+1)],dx=b[0]-a[0],dz=b[1]-a[1],L=Math.hypot(dx,dz)||1,nx=-dz/L,nz=dx/L;for(const s of[-1,1]){const x=points[i][0]+nx*width*.5*s,z=points[i][1]+nz*width*.5*s;pos.push(x,terrainY(x,z)+offset,z);}if(i<points.length-1){const n=i*2;ix.push(n,n+2,n+1,n+2,n+3,n+1)}}const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.Float32BufferAttribute(pos,3));g.setIndex(ix);g.computeVertexNormals();return mesh(g,mat)}
function waterRibbon(spec,width=spec.w){const points=spec.p,pos=[],ix=[],lens=[];let total=0;for(let i=0;i<points.length-1;i++){const l=Math.hypot(points[i+1][0]-points[i][0],points[i+1][1]-points[i][1]);lens.push(l);total+=l}let acc=0;for(let i=0;i<points.length;i++){const a=points[Math.max(0,i-1)],b=points[Math.min(points.length-1,i+1)],dx=b[0]-a[0],dz=b[1]-a[1],L=Math.hypot(dx,dz)||1,nx=-dz/L,nz=dx/L,t=i===points.length-1?1:acc/(total||1),y=channelSurface(spec,t)+.02;for(const side of[-1,1])pos.push(points[i][0]+nx*width*.5*side,y,points[i][1]+nz*width*.5*side);if(i<points.length-1){const n=i*2;ix.push(n,n+2,n+1,n+2,n+3,n+1);acc+=lens[i]}}const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.Float32BufferAttribute(pos,3));g.setIndex(ix);g.computeVertexNormals();return mesh(g,MAT.water)}
for(const spec of channelSpecs){if(spec.p!==sourcePath)ribbon2D(spec.p,spec.w*2.05,MAT.wet,.015);waterRibbon(spec,spec.w*.92)}
const river=[];for(let i=0;i<=150;i++){const x=-215+i*430/150;river.push([x,riverZ(x)])}ribbon2D(river,20.5,MAT.water,.08);ribbon2D(river.map(p=>[p[0],p[1]-12]),2.4,MAT.wet,.03);ribbon2D(river.map(p=>[p[0],p[1]+12]),2.4,MAT.wet,.03);
const stageSurface=[MAT.water,MAT.water,new THREE.MeshStandardMaterial({color:'#6b8144',roughness:.9}),new THREE.MeshStandardMaterial({color:'#8e8048',roughness:.92})];for(const f of flatFields){const sh=new THREE.Shape(f.poly.map(p=>new THREE.Vector2(p[0],p[1]))),g=new THREE.ShapeGeometry(sh);g.rotateX(-Math.PI/2);const m=mesh(g,stageSurface[f.stage]);m.position.y=f.bed+(f.waterDepth||0)+.018;}
function makeBundLine(a,b){const pts=[];for(let i=0;i<=22;i++){const t=i/22;pts.push([mix(a[0],b[0],t),mix(a[1],b[1],t)])}ribbon2D(pts,1.55,MAT.soil,.18);ribbon2D(pts,.72,MAT.grass,.44)}for(let j=0;j<nodes.length;j++)for(let i=0;i<nodes[j].length-1;i++)makeBundLine(nodes[j][i],nodes[j][i+1]);for(let i=0;i<nodes[0].length;i++)for(let j=0;j<nodes.length-1;j++)makeBundLine(nodes[j][i],nodes[j+1][i]);
const crossXs=[-105,-70,-24,22,68];for(let q=0;q<crossXs.length;q++){const pts=[];for(let z=-118;z<10;z+=4){const x=crossXs[q]+7*Math.sin((z+q*19)*.035)+2*Math.sin(z*.11+q);if(terraceMask(x,z)>.5)pts.push([x,z]);}if(pts.length>2){ribbon2D(pts,1.15,MAT.soil,.17);ribbon2D(pts,.52,MAT.grass,.39)}}
Object.assign(S,{terrain,ribbon2D,waterRibbon,river});import('./ecology.mjs');
