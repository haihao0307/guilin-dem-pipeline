import fs from 'node:fs';
import * as K from './r045_round69_kernel.mjs';
import * as R68 from '../round-68/r045_round68_kernel.mjs';
import * as R47 from '../round-47/r045_round47_kernel.mjs';
import * as R30 from '../round-30/r045_round30_kernel.mjs';
const checks=[];const add=(name,pass,value,limit)=>checks.push({name,pass:Boolean(pass),value,limit});
const xs=[];for(let x=-216;x<=114;x+=6)xs.push(x);const zs=[];for(let z=-126;z<=6;z+=6)zs.push(z);
function receiverBand(x,z){return Math.abs(z-R30.riverZ(x))<=R30.riverW(x)+12}
let activeN=0,activeP=0,maskErr=0,stepErr=0,phaseErr=0,indexErr=0,baseErr=0,groupErr=0,thresholdCross=0;
let candidateTotal=0,acceptedNode=0,changed=0,unsupportedChanged=0,inactiveChanged=0,hardChanged=0,receiverChanged=0,strongChanged=0,maxChange=0;
const groups=[0,0,0],changedGroups=new Set(),changedPts=[];
for(const x of xs)for(const z of zs){const n=K.terraceStateAt(x,z),p=R68.terraceStateAt(x,z),b=R47.terraceStateAt(x,z),ev=K.r69NodeEvidenceAt(x,z),comp=K.r69ComponentAt(x,z),d=Math.abs(n.delta-p.delta),dd=R30.nearestExtendedDrainageDistance(x,z);if(n.mask>.12){activeN++;if(n.groupIndex>=0&&n.groupIndex<3)groups[n.groupIndex]++}if(p.mask>.12)activeP++;if((n.mask>.12)!==(p.mask>.12))thresholdCross++;maskErr=Math.max(maskErr,Math.abs(n.mask-b.mask));stepErr=Math.max(stepErr,Math.abs(n.step-b.step));phaseErr=Math.max(phaseErr,Math.abs(n.phase-b.phase));indexErr=Math.max(indexErr,Math.abs(n.index-b.index));baseErr=Math.max(baseErr,Math.abs(n.base-b.base));if(n.groupIndex!==b.groupIndex)groupErr++;if(ev.ok){candidateTotal++;if(comp.accepted)acceptedNode++}if(d>1e-6){changed++;maxChange=Math.max(maxChange,d);if(!ev.ok||!comp.accepted||K.r69AcceptanceWeightAt(x,z)<.999999)unsupportedChanged++;if(b.mask<=.12)inactiveChanged++;if(dd<=12)hardChanged++;if(receiverBand(x,z))receiverChanged++;if(b.mask>=.82)strongChanged++;changedGroups.add(b.groupIndex);changedPts.push({x,z,change:n.delta-p.delta,mask:b.mask,groupIndex:b.groupIndex,index:b.index,componentSize:comp.size,componentSpan:comp.span,coherence:ev.ev?.coherence||0,runSpan:ev.ev?.span||0,dd})}}
const summary=K.r69GraphSummary(),acceptedComponents=summary.components.filter(c=>c.accepted),largestEvidence=acceptedComponents[0]||{size:0,span:0};
const key=(x,z)=>`${x},${z}`,map=new Map(changedPts.map((p,i)=>[key(p.x,p.z),i])),seen=new Set(),changeComps=[];for(let i=0;i<changedPts.length;i++){if(seen.has(i))continue;const q=[i],ids=[];seen.add(i);while(q.length){const a=q.pop(),p=changedPts[a];ids.push(a);for(const dx of [-6,0,6])for(const dz of [-6,0,6]){if(!dx&&!dz)continue;const j=map.get(key(p.x+dx,p.z+dz));if(j===undefined||seen.has(j))continue;const r=changedPts[j];if(r.groupIndex!==p.groupIndex||Math.abs(r.index-p.index)>1)continue;seen.add(j);q.push(j)}}changeComps.push(ids)}changeComps.sort((a,b)=>b.length-a.length);
const physicalSpan=ids=>{let m=0;for(const i of ids)for(const j of ids){const a=changedPts[i],b=changedPts[j];m=Math.max(m,Math.hypot(a.x-b.x,a.z-b.z))}return m};const largestChanged=changeComps[0]?.length||0,largestChangedSpan=changeComps[0]?physicalSpan(changeComps[0]):0;
let maxInc=0,maxIncAt=null;for(const p of changedPts){const c=K.terraceDelta(p.x,p.z);for(const[xx,zz]of[[p.x+3,p.z],[p.x-3,p.z],[p.x,p.z+3],[p.x,p.z-3]]){const inc=Math.abs(K.terraceDelta(xx,zz)-c);if(inc>maxInc){maxInc=inc;maxIncAt={x:p.x,z:p.z,xx,zz,inc}}}}
let outside=0,hard=0,receiver=0;for(let x=-216;x<=216;x+=36){for(const z of [-300,-220,-170,-145,30,70,130])outside=Math.max(outside,Math.abs(K.terraceDelta(x,z)-R68.terraceDelta(x,z)));for(let z=-126;z<=6;z+=18)if(R30.nearestExtendedDrainageDistance(x,z)<=12)hard=Math.max(hard,Math.abs(K.terraceDelta(x,z)-R68.terraceDelta(x,z)));const rz=R30.riverZ(x);for(const dz of [-8,0,8])receiver=Math.max(receiver,Math.abs(K.terraceDelta(x,rz+dz)-R68.terraceDelta(x,rz+dz)))}
const sourceSha=process.env.GITHUB_SHA||null;
add('version',K.VERSION==='R045.69',K.VERSION,'R045.69');add('qa_contract',K.R69_CONTRACT==='R045.69-macro-contour-ribbon-evidence-v1',K.R69_CONTRACT,'exact contract id');add('source_sha_present',typeof sourceSha==='string'&&sourceSha.length>=7,sourceSha,'workflow source commit recorded');
add('water_graph_identity',JSON.stringify(K.nodes)===JSON.stringify(R68.nodes)&&JSON.stringify(K.edges)===JSON.stringify(R68.edges),{nodes:K.nodes.length,edges:K.edges.length},'exact inherited graph');add('plan_frame_exact',maskErr===0&&stepErr===0&&phaseErr===0&&indexErr===0&&baseErr===0&&groupErr===0,{maskErr,stepErr,phaseErr,indexErr,baseErr,groupErr},'exact frozen R47/R68 mask/group/step/phase/index/base');add('active_footprint_exact',activeN===activeP&&thresholdCross===0,{activeN,activeP,thresholdCross},'exact R68 active footprint');
add('macro_evidence_material',candidateTotal>=20&&acceptedNode>=12&&acceptedComponents.length>=1,{candidateTotal,acceptedNode,acceptedComponents:acceptedComponents.length},'>=20 frozen contour-run candidates, >=12 accepted ribbon nodes, >=1 accepted component');add('macro_evidence_span',largestEvidence.size>=6&&largestEvidence.span>=30,largestEvidence,'largest frozen contour ribbon >=6 nodes and >=30m physical span');
add('profile_change_material',changed>=8&&maxChange>.004,{changed,maxChange},'>=8 physical changes and max change >4mm');add('multi_family_change',changedGroups.size>=2,[...changedGroups],'>=2 terrace families participate');add('changed_run_material',largestChanged>=3&&largestChangedSpan>=12,{largestChanged,largestChangedSpan,changeComponents:changeComps.length},'physical changes include a compatible >=3-cell / >=12m run');
add('all_changes_supported',unsupportedChanged===0,unsupportedChanged,'every changed 6m node belongs to precomputed accepted macro ribbon evidence');add('inactive_exact',inactiveChanged===0,inactiveChanged,'zero change at inherited mask <=0.12');add('strong_exact',strongChanged===0,strongChanged,'zero change at inherited mask >=0.82');add('hard_drainage_exact',hardChanged===0&&hard<1e-12,{hardChanged,hard},'zero change in <=12m drainage core');add('foreground_receiver_exact',receiverChanged===0&&receiver<1e-12,{receiverChanged,receiver},'zero change in receiver +12m band');add('outside_exact',outside<1e-9,outside,'zero outside terrace support domain');add('height_change_bounded',maxChange<=.0350001,maxChange,'R69-R68 <=0.035m per authoritative node');add('three_meter_cliff_gate',maxInc<1.05,{maxInc,maxIncAt},'<1.05m terrace increment / 3m at every changed 6m node');add('three_groups_retained',groups.every(v=>v>20),groups,'three terrace families retained');
add('locks_retained',K.snapshot.visualAcceptance===false&&K.snapshot.parcelGenerationEnabled===false&&K.snapshot.waterStateKnown===false&&K.snapshot.productionReady===false,{visual:K.snapshot.visualAcceptance,parcel:K.snapshot.parcelGenerationEnabled,water:K.snapshot.waterStateKnown,production:K.snapshot.productionReady},'all false pending fixed-view review');add('logic_fallacy_explicit',K.snapshot.round69?.logicCorrection?.includes('does not imply')&&K.snapshot.round69?.logicCorrection?.includes('false bridges'),K.snapshot.round69?.logicCorrection,'local connected residuals != hillside-scale organization; longer graph != valid contour evidence');add('field_constraint_explicit',K.snapshot.round69?.constraint?.includes('synthetic morphology/QA')&&K.snapshot.round69?.constraint?.includes('cannot recover'),K.snapshot.round69?.constraint,'field truth missing');add('xiaoma_boundary_retained',K.snapshot.round69?.xiaomaBoundary?.includes('do not establish'),K.snapshot.round69?.xiaomaBoundary,'geometry != hydraulic truth');add('mrrolord_ordering_only',K.snapshot.round69?.mrRolordUse?.includes('hydrology-first ordering')&&K.snapshot.round69?.mrRolordUse?.includes('not agricultural truth'),K.snapshot.round69?.mrRolordUse,'ordering only');add('reference_reread_honest',K.snapshot.round69?.referenceUse?.includes('re-resolved and reread')&&K.snapshot.round69?.referenceUse?.includes('non-metric'),K.snapshot.round69?.referenceUse,'exact image reread; non-metric use only');
const result={version:K.VERSION,qaContract:K.R69_CONTRACT,sourceSha,passed:checks.every(c=>c.pass),gateCount:checks.length,passedCount:checks.filter(c=>c.pass).length,checks,metrics:{activeN,activeP,groups,candidateTotal,acceptedNode,acceptedComponents:acceptedComponents.length,largestEvidence,changed,changedGroups:[...changedGroups],largestChanged,largestChangedSpan,changeComponentCount:changeComps.length,maxChange,maxInc,maxIncAt,inactiveChanged,unsupportedChanged,hardChanged,receiverChanged,strongChanged,outside,hard,receiver,changedPoints:changedPts,graphSummary:{candidateNodes:summary.candidateNodes,acceptedNodes:summary.acceptedNodes,rejectedNodes:summary.rejectedNodes,acceptedComponents:summary.acceptedComponents,components:summary.components.slice(0,20)},qaScope:'authoritative 6m whole-slope macro contour-ribbon evidence over frozen R47/R68 footprint; tangent-aligned connected evidence + bounded profile expression + exact drainage/footprint locks + 3m local cliff checks + source provenance'}};fs.writeFileSync(new URL('./r045_round69_qa_result.json',import.meta.url),JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));if(!result.passed)process.exitCode=2;
