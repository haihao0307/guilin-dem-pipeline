import fs from 'node:fs';
import * as K from './r045_round37_kernel.mjs';
import * as R35 from '../round-35/r045_round35_kernel.mjs';
import * as R30 from '../round-30/r045_round30_kernel.mjs';
import {SEEDS,META} from './r045_round37_seed_cache.mjs';
const checks=[];const add=(name,pass,value,limit)=>checks.push({name,pass:Boolean(pass),value,limit});
const median=a=>{if(!a.length)return 0;const b=[...a].sort((x,y)=>x-y),m=b.length>>1;return b.length%2?b[m]:(b[m-1]+b[m])/2};
add('version',K.VERSION==='R045.37',K.VERSION,'R045.37');
add('seed_cache_generated',META.generated===true&&META.positiveSeedCount>=4,META,'generated cache with >=4 verified positive seeds');
add('water_graph_identity_preserved',JSON.stringify(K.nodes)===JSON.stringify(R35.nodes)&&JSON.stringify(K.edges)===JSON.stringify(R35.edges),{nodes:[R35.nodes.length,K.nodes.length],edges:[R35.edges.length,K.edges.length]},'exact inherited graph');
add('terrain_carriers_identity_preserved',JSON.stringify(K.terrainChannels)===JSON.stringify(R35.terrainChannels)&&JSON.stringify(K.outletContinuum)===JSON.stringify(R35.outletContinuum),{terrainChannels:K.terrainChannels.length,outlets:K.outletContinuum.length},'exact inherited carriers');

const rows=[];for(let z=-132;z<=10;z+=6){const row=[];for(let x=-220;x<=120;x+=6)row.push({x,z,n:K.terraceStateAt(x,z),o:R35.terraceStateAt(x,z),dd:R30.nearestExtendedDrainageDistance(x,z)});rows.push(row)}
function shape(row,key){let runs=0,best=0,cur=0;for(const c of row){const v=c[key].mask>.12;if(v){if(!cur)runs++;cur++;best=Math.max(best,cur)}else cur=0}return{runs,best:best*6}}
let dmax=0,change35=0,active=0,oldActive=0,core=0,oldCore=0,gainCells=0,thresholdCross=0,hardCoreActive=0,hardCoreDelta=0,unsafe=0,maxGain=0,rowRunsN=0,rowRunsO=0;const groups=[0,0,0],longN=[],longO=[];
for(const row of rows){const a=shape(row,'n'),b=shape(row,'o');rowRunsN+=a.runs;rowRunsO+=b.runs;if(a.best)longN.push(a.best);if(b.best)longO.push(b.best);for(const c of row){const n=c.n,o=c.o,g=n.mask-o.mask;dmax=Math.max(dmax,Math.abs(n.delta));change35=Math.max(change35,Math.abs(n.delta-o.delta));if(n.mask>.12){active++;groups[n.groupIndex]++}if(o.mask>.12)oldActive++;if(n.mask>.60)core++;if(o.mask>.60)oldCore++;if(g>.004){gainCells++;maxGain=Math.max(maxGain,g);if(o.mask<=.12&&n.mask>.12)thresholdCross++;const riverGap=Math.abs(c.z-R30.riverZ(c.x));if(c.dd<=12||R35.broadTerraceEligibility(c.x,c.z)<.03||R35.terraceGroupEnvelope(c.x,c.z)<.035||riverGap<=R30.riverW(c.x)+18)unsafe++;}if(c.dd<=12){if(n.mask>.01)hardCoreActive++;hardCoreDelta=Math.max(hardCoreDelta,Math.abs(n.delta))}}}
const burdenN=rowRunsN/(active||1),burdenO=rowRunsO/(oldActive||1);
const geom={deltaMax:dmax,changeFrom35:change35,active,oldActive,core,oldCore,gainCells,thresholdCross,maxGain,unsafe,hardCoreActive,hardCoreDelta,groups,rowRunsN,rowRunsO,fragmentationBurdenN:burdenN,fragmentationBurdenO:burdenO,medianLongestN:median(longN),medianLongestO:median(longO),maxLongestN:Math.max(...longN),maxLongestO:Math.max(...longO)};
add('terrace_geometry_substantive_bounded',dmax>.20&&dmax<.95,geom,'.20m < max delta < .95m');
add('cached_field_is_material',gainCells>20&&maxGain>.01,{gainCells,maxGain},'>20 samples gain >.004 and max gain >.01');
add('cached_field_changes_topology',thresholdCross>2,{thresholdCross},'>2 weak inherited samples cross .12');
add('r37_change_from_r35_bounded',change35>.005&&change35<.40,change35,'.005m < sampled max change < .40m');
add('support_does_not_shrink',active>=oldActive,{active,oldActive},'active support >= R35');
add('core_does_not_shrink',core>=oldCore,{core,oldCore},'core support >= R35');
add('fragmentation_burden_improves_or_holds',burdenN<=burdenO*1.01,{burdenN,burdenO},'runs/active <=1.01x R35');
add('longest_ribbons_hold_or_improve',median(longN)>=median(longO),{medianNew:median(longN),medianOld:median(longO),maxNew:Math.max(...longN),maxOld:Math.max(...longO)},'median longest run >= R35');
add('all_three_groups_remain_material',groups.every(n=>n>20),groups,'each group >20 active samples');
add('cached_candidates_stay_safe',unsafe===0,{unsafe},'no gained sample in drainage core/outside eligibility/receiver');
add('hard_drainage_core_empty',hardCoreActive===0&&hardCoreDelta<1e-9,{hardCoreActive,hardCoreDelta},'zero support/delta <=12m drainage');

let seedRetained=0,seedCompared=0,maxSeedLoss=0;for(const [key,s] of Object.entries(SEEDS)){if(s.gain<=.004)continue;seedCompared++;const [ix,iz]=key.split(',').map(Number),x=META.x0+ix*META.dx,z=META.z0+iz*META.dz,a=R35.terraceStateAt(x,z),n=K.terraceStateAt(x,z);if(n.mask>a.mask+.002)seedRetained++;maxSeedLoss=Math.max(maxSeedLoss,Math.max(0,s.mask36-n.mask))}add('r36_verified_seed_signal_retained',seedCompared>5&&seedRetained>=Math.ceil(seedCompared*.65),{seedCompared,seedRetained,maxSeedLoss},'>5 persisted R36-positive seeds and >=65% retain positive R37 support');

let frameN=0,maxStep=0,maxPhase=0,maxRaw=0;for(let x=-210;x<=110;x+=16)for(let z=-128;z<=2;z+=10){const n=K.terraceStateAt(x,z),o=R35.terraceStateAt(x,z);frameN++;maxStep=Math.max(maxStep,Math.abs(n.step-o.step));maxPhase=Math.max(maxPhase,Math.abs(n.phase-o.phase));maxRaw=Math.max(maxRaw,Math.abs(n.raw-o.raw))}add('r35_stair_frame_exact',maxStep<1e-12&&maxPhase<1e-12&&maxRaw<1e-12,{frameN,maxStep,maxPhase,maxRaw},'step/phase/raw exactly inherited');

const br=[],rr=[],bs=[],rs=[];let bn=0,rn=0;for(let x=-204;x<=108;x+=12)for(let z=-126;z<=2;z+=8){const st=K.terraceStateAt(x,z);if(st.mask<.68||R30.nearestExtendedDrainageDistance(x,z)<18)continue;const bg=R30.gradient(x,z).mag,ng=K.gradient(x,z).mag;if(bg<.03||bg>.60)continue;if(st.frac<.25||st.frac>.75){bn++;br.push(ng/bg);bs.push(ng)}if(st.frac>.46&&st.frac<.54){rn++;rr.push(ng/bg);rs.push(ng)}}const terr={benchN:bn,riserN:rn,benchRatioMedian:median(br),riserRatioMedian:median(rr),benchSlopeMedian:median(bs),riserSlopeMedian:median(rs)};add('bench_riser_samples_exist',bn>10&&rn>3,terr,'>10 bench and >3 riser samples');add('benches_flatter_than_r30',median(br)<.90,terr,'bench/base median <.90');add('risers_steeper_than_benches',median(rs)>median(bs)*1.25,terr,'riser slope >1.25x bench');add('risers_steepen_vs_r30',median(rr)>1.08,terr,'riser/base median >1.08');

let far=0,near=0,receiver=0,outside=0;for(let x=-216;x<=216;x+=18){for(const z of [-300,-220,-170,-145])far=Math.max(far,Math.abs(K.terraceDelta(x,z)));for(const z of [-300,-220,-170,-145,30,70,130])outside=Math.max(outside,Math.abs(K.terraceDelta(x,z)));for(let z=-126;z<=0;z+=12)if(R30.nearestExtendedDrainageDistance(x,z)<=12)near=Math.max(near,Math.abs(K.terraceDelta(x,z)));const rz=R30.riverZ(x);for(const dz of [-8,-2,0,2,8])receiver=Math.max(receiver,Math.abs(K.terraceDelta(x,rz+dz)))}add('far_upstream_untouched',far<1e-9,far,'0');add('support_localized',outside<1e-9,outside,'0');add('drainage_core_protected',near<1e-9,near,'0');add('foreground_receiver_unchanged',receiver<1e-9,receiver,'0');
let maxInc=0;for(let x=-210;x<=110;x+=12)for(let z=-126;z<=0;z+=6)maxInc=Math.max(maxInc,Math.abs(K.terraceDelta(x,z+6)-K.terraceDelta(x,z)));add('terrace_increment_not_cliff',maxInc<1.25,maxInc,'<1.25m per 6m');
add('visual_acceptance_locked',K.snapshot.visualAcceptance===false,K.snapshot.visualAcceptance,false);add('parcel_locked',K.snapshot.parcelGenerationEnabled===false,K.snapshot.parcelGenerationEnabled,false);add('water_unknown',K.snapshot.waterStateKnown===false,K.snapshot.waterStateKnown,false);add('production_locked',K.snapshot.productionReady===false,K.snapshot.productionReady,false);
add('logic_error_explicit',K.snapshot.round37?.logicCorrection?.includes('does not imply visual acceptance')&&K.snapshot.round37?.logicCorrection?.includes('timeout'),K.snapshot.round37?.logicCorrection,'numeric pass != visual pass; timeout != raise timeout');
add('real_world_constraint_explicit',K.snapshot.round37?.constraint?.includes('12.5 m')&&K.snapshot.round37?.constraint?.includes('cannot supply'),K.snapshot.round37?.constraint,'missing field truth explicit');
add('xiaoma_boundary_retained',K.snapshot.round37?.xiaomaBoundary?.includes('not ownership')||K.snapshot.round37?.xiaomaBoundary?.includes('not ownership or hydraulic'),K.snapshot.round37?.xiaomaBoundary,'surface continuity != hydraulic truth');
add('mrrolord_ordering_only',K.snapshot.round37?.mrRolordUse?.includes('drainage hierarchy')&&K.snapshot.round37?.mrRolordUse?.includes('No Blender'),K.snapshot.round37?.mrRolordUse,'ordering only');
add('reference_nonmetric',K.snapshot.round37?.referenceUse?.includes('reopened')&&K.snapshot.round37?.referenceUse?.includes('no metric'),K.snapshot.round37?.referenceUse,'visual morphology only');
const result={version:K.VERSION,passed:checks.every(c=>c.pass),gateCount:checks.length,passedCount:checks.filter(c=>c.pass).length,checks,metrics:{geometry:geom,terraceMorphology:terr,seedCache:META,seedRetained,seedCompared,maxSeedLoss,far,near,receiver,outside,maxInc}};fs.writeFileSync(new URL('./r045_round37_qa_result.json',import.meta.url),JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));if(!result.passed)process.exitCode=2;
