import fs from 'node:fs';
const q=JSON.parse(fs.readFileSync(new URL('./r045_round87_qa_result.json',import.meta.url),'utf8'));
if(!q.audit?.plan||!q.audit?.rivers||!q.audit?.before||!q.audit?.after)throw new Error('R87 audit inputs missing');
const A=q.audit,m=q.metrics||{},before=A.before,after=A.after,nz=before.length-1,nx=before[0].length-1;
let mn=Infinity,mx=-Infinity,maxDense=0,denseChanged=0;for(let j=0;j<=nz;j++)for(let i=0;i<=nx;i++){mn=Math.min(mn,before[j][i],after[j][i]);mx=Math.max(mx,before[j][i],after[j][i]);const d=Math.abs(after[j][i]-before[j][i]);maxDense=Math.max(maxDense,d);if(d>.0005)denseChanged++}
const data={version:q.version,qaContract:q.qaContract,sourceSha:q.sourceSha,terrain:{x0:A.x0,x1:A.x1,z0:A.z0,z1:A.z1,nx,nz,before,after,sharedMin:mn,sharedMax:mx},rivers:A.rivers,planSpec:A.planSpec,plan:A.plan,exactChanges:m.changedPoints||[],nodeChanges:m.nodeChangedPoints||[],benchPairs:m.benchPairs||[],inheritedMax:m.maxBeforeAt||null,state:{qaPassed:q.passed===true,sampleCount:m.sampleCount||0,changedQueries:m.changedQueries||0,nodeChanged:m.nodeChanged||0,groups:m.changedGroups||[],benchPairs:m.benchPairCount||0,benchBefore:m.benchBeforeMean||0,benchAfter:m.benchAfterMean||0,benchImprovement:m.benchImprovement||0,energyRatio:m.benchEnergyRatio||0,improvedPairs:m.improvedPairs||0,worsenedPairs:m.worsenedPairs||0,maxChange:m.maxChange||0,maxBeforeInc:m.maxBeforeInc||0,maxAfterInc:m.maxAfterInc||0,inheritedUnsafeTouched:m.inheritedUnsafeTouched||0,maxDense,denseChanged,visualAcceptance:false},generatedAt:new Date().toISOString(),source:'R045.87 fixed-view diagnostic: accepted R045.86 versus pairwise-monotone broad-bench extremum relaxation. A/B use the exact authoritative raster with one shared range. Orange marks physical query changes; magenta marks changed authority nodes. The inherited global maximum is reported separately because aggregate flattening cannot classify a riser as an error. Morphology evidence only; no parcel or hydraulic truth.'};
fs.writeFileSync(new URL('./r045_round87_audit_cache.mjs',import.meta.url),`export const AUDIT=${JSON.stringify(data)};\n`);
console.log(JSON.stringify(data.state,null,2));