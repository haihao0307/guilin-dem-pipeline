# R045.43 topology QA correction

R045.42 proved that a browser pass can coexist with a geometry no-op. R045.43 attempt A then proved that the proposed two-sided same-row closure class produced no geometry. Attempt B created three real, safe frozen-source branch companions, but the inherited `rowRuns / activeCells` gate still failed.

That ratio is not a topology-identifying fragmentation measure after R041 deliberately introduced adjacent-row branches. A valid branch can increase row-run count, while simply appending cells to existing runs lowers the ratio without reconnecting topology. Treating the ratio as truth would therefore reward denominator growth and could force geometry to chase the metric.

The geometry is not changed for this correction: R045.43 still promotes only one non-recursive same-row companion beside an already-active R041 `familyRepairGain` source, with exact R042 active-surface inheritance, exact step/phase/raw response, unchanged 0.84 amplitude, hard drainage protection and receiver protection.

The numeric gate now evaluates same-family/stair-compatible 8-neighbour connected components, orphan cells and tiny components against both R042 and R040. Raw row-run count and `rowRuns / activeCells` remain recorded as diagnostics; row-run count is still forbidden to worsen versus R042. This avoids lowering a threshold while also avoiding a metric that can be gamed without topology repair.

Evidence class remains synthetic morphology/QA. The 6 m lattice, branch companion, stair tolerances and 12 m hard drainage core are not surveyed Yunnan agricultural dimensions or measured hydraulic connectivity.
