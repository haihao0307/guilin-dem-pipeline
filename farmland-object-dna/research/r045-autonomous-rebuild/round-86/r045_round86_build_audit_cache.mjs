import fs from 'node:fs';
const q=JSON.parse(fs.readFileSync(new URL('./r045_round86_qa_result.json',import.meta.url),'utf8'));
if(!q.audit?.plan||!q.audit?.rivers||!q.audit?.before||!q.audit?.after)throw new Error('R86 audit inputs missing');
const A=q.audit,m=q.metrics||{},before=A.before,after=A.after,nz=before.length-1,nx=before[0].length-1;
let mn=Infinity,mx=-Infinity,maxDense=0,denseChanged=0;for(let j=0;j<=nz;j++)for(let i=0;i<=nx;i++){mn=Math.min(mn,before[j][i],after[j][i]);mx=Math.max(mx,before[j][i],after[j][i]);const d=Math.abs(after[j][i]-before[j][i]);maxDense=Math.max(maxDense,d);if(d>.0005)denseChanged++}
const data={version:q.version,qaContract:q.qaContract,sourceSha:q.sourceSha,terrain:{x0:A.x0,x1:A.x1,z0:A.z0,z1:A.z1,nx,nz,before,after,sharedMin:mn,sharedMax:mx},rivers:A.rivers,planSpec:A.planSpec,plan:A.plan,exactChanges:m.changedPoints||[],nodeChanges:m.nodeChangedPoints||[],benchPairs:m.benchPairs||[],state:{qaPassed:q.passed===true,candidates:m.candidates||0,sampleCount:m.sampleCount||0,changedQueries:m.changedQueries||0,nodeChanged:m.nodeChanged||0,changedGroups:m.changedGroups||[],opportunityGroups:m.opportunityGroups||[],benchPairs:m.benchPairCount||0,benchBefore:m.benchBeforeMean||0,benchAfter:m.benchAfterMean||0,benchRatio:m.benchGradientRatio||0,energyRatio:m.benchEnergyRatio||0,improvedPairs:m.improvedPairs||0,worsenedPairs:m.worsenedPairs||0,balanceRatio:m.balanceRatio||0,maxChange:m.maxChange||0,projected:m.projected||0,changedPairCount:m.changedPairCount||0,maxChangedAfter:m.maxChangedAfter||0,maxBeforeInc:m.maxBeforeInc||0,maxAfterInc:m.maxAfterInc||0,maxGlobalWorsening:m.maxGlobalWorsening||0,newProofViolation:m.newProofViolation||0,newCliffViolation:m.newCliffViolation||0,inheritedUnsafeTouched:m.inheritedUnsafeTouched||0,inheritedUnsafeWorsened:m.inheritedUnsafeWorsened||0,maxDense,denseChanged,visualAcceptance:false},generatedAt:new Date().toISOString(),source:'R045.86 fixed-view diagnostic: accepted R045.84 versus identity-aware broad-bench Laplacian relaxation. A/B render the exact authoritative 6 m numeric-audit raster with one shared height range and identical hillshade. Orange marks actual physical query changes; magenta marks changed nodes. Safety summary distinguishes R86-touched pairs from inherited predecessor pairs, so an inherited out-of-scope cliff cannot be misreported as a new R86 violation. This view is morphology evidence only and does not establish parcel or hydraulic truth.'};
fs.writeFileSync(new URL('./r045_round86_audit_cache.mjs',import.meta.url),`export const AUDIT=${JSON.stringify(data)};\n`);
console.log(JSON.stringify(data.state,null,2));
