1;i++){const p=[[macroCuts[i],PLAIN.zmin],[macroCuts[i+1],PLAIN.zmin],[macroCuts[i+1],PLAIN.zmax],[macroCuts[i],PLAIN.zmax]];subdivideParcel(p,0,i*.83,latent);}return latent.map((latentPoly,i)=>{const poly=densifyWarpPolygon(latentPoly),c=centroid(poly),A=area(poly),stage=(i+Math.floor(c[1]/19)+Math.floor((c[0]+128)/37))%4;return{id:`PF${String(i+1).padStart(3,'0')}`,kind:'plain',latentPoly,poly,centroid:c,area:A,bed:.94-.0044*(c[1]-18)+.024*Math.sin(c[0]*.035),stage,stageName:['transplanted','tillering','heading','ripening'][stage],waterDepth:[.045,.06,.025,0][stage]};});}
const plainFields=buildPlainFields();
function plainFieldAt(x,z){for(const f of plainFields)if(pointInPoly(x,z,f.poly))return f;return null;}
function latentFieldAt(x,z){for(const f of plainFields)if(pointInPoly(x,z,f.latentPoly))return f;return null;}
function densifyWarpLine(a,b,spacing=3.2){const L=Math.hypot(b[0]-a[0],b[1]-a[1]),n=Math.max(1,Math.ceil(L/spacing)),points=[];for(let i=0;i<=n;i++){const t=i/n;points.push(warpPoint(mix(a[0],b[0],t),mix(a[1],b[1],t)));}return points;}
function buildPlainBundLines(){const lines=[];for(const seg of latentBundSegments){const dx=seg.b[0]-seg.a[0],dz=seg.b[1]-seg.a[1],L=Math.hypot(dx,dz)||1,nx=-dz/L,nz=dx/L,m=[(seg.a[0]+seg.b[0])*.5,(seg.a[1]+seg.b[1])*.5],eps=.08,left=latentFieldAt(m[0]+nx*eps,m[1]+nz*eps),right=latentFieldAt(m[0]-nx*eps,m[1]-nz*eps),owners=[];if(left)owners.push(left.id);if(right&&(!left||right.id!==left.id))owners.push(right.id);lines.push({latentA:seg.a,latentB:seg.b,points:densifyWarpLine(seg.a,seg.b),owners,kind:seg.kind});}return lines;}
const plainBundLines=buildPlainBundLines();
const plainEdges=plainBundLines.flatMap(line=>line.points.slice(0,-1).map((a,i)=>({a,b:line.points[i+1],owners:line.owners,kind:line.kind,line})));

const mountains=[[-176,-236,58,52,48],[-122,-254,72,58,52],[-63,-235,64,55,48],[-5,-267,86,68,58],[62,-244,69,60,52],[124,-258,77,64,55],[182,-226,59,55,48],[-206,-168,52,52,46],[194,-170,54,52,48],[-150,-188,43,45,42],[142,-190,47,48,43]];
function mountainField(x,z){let h=0;for(let i=0;i<mountains.length;i++){const[cx,cz,H,rx,rz]=mountains[i],dx=(x-cx)/rx,dz=(z-cz)/rz,theta=Math.atan2(dz,dx),warp=1+.14*Math.sin(theta*3+i*.7)+.055*Math.sin(theta*7-i*.4),rr=Math.hypot(dx,dz)/warp;if(rr<1.5){const core=Math.pow(Math.max(0,1-Math.pow(rr,1.55)),.58),ribs=.78+.22*ridged(x*.026+i*.7,z*.022-i*.3);h=Math.max(h,H*core*ribs);}}return h;}
function baseNatural(x,z){const back=1-sm(-145,38,z);let y=.35+24*Math.pow(back,1.34);y+=geomDetail(x,z)*(1.3+.8*back);y+=mountainField(x,z);const valley=Math.exp(-Math.pow((x+4+10*Math.sin(z*.009))/92,2))*(1-sm(-260,-105,z));y-=valley*(2.2+5.2*back);return y;}
function terraceMask(x,z){let m=sm(-150,-122,x)*(1-sm(92,116,x))*sm(-142,-120,z)*(1-sm(17,34,z));const g1=Math.exp(-Math.pow((x+52+.08*(z+70))/9,2)),g2=Math.exp(-Math.pow((x-46-.05*(z+70))/10,2));m*=1-clamp(g1*.93+g2*.82,0,.96);return m;}
function terraceInfoFromNatural(nat,x,z){const step=1.48,warp=.36*Math.sin(x*.031)+.22*Math.sin((x+z)*.019)+.24*(noise(x*.017,z*.017)-.5),q=(nat+warp)/step,k=Math.floor(q),f=q-k,rise=sm(.79,.975,f);let y=(k+rise)*step;const bund=.28*Math.exp(-Math.pow((f-.72)/.035,2));y+=bund;return{y,k,f,warp,bund};}
function traceTerraceBand(k,x0,x1){const top=[],bottom=[];for(let xi=0;xi<=20;xi++){const x=mix(x0,x1,xi/20);let minZ=Infinity,maxZ=-Infinity;for(let z=-138;z<=24;z+=1.1){if(terraceMask(x,z)<.45)continue;const ti=terraceInfoFromNatural(baseNatural(x,z),x,z);if(ti.k===k){minZ=Math.min(minZ,z);maxZ=Math.max(maxZ,z);}}if(Number.isFinite(minZ)){top.push([x,minZ]);bottom.push([x,maxZ]);}}if(top.length<3)return null;return dedupePoly(top.concat(bottom.reverse()));}
function buildTerraceFields(){const fields=[];let id=1;for(let k=1;k<=17;k++){const cuts=[-118];let x=-118,s=0;while(x<88){const step=21+14*((microscopeField(x,k*11,k*.29,4,.007)+1)*.5)+((k+s)%3===0?5:0);x+=step;if(x<90)cuts.push(x);s++;}cuts.push(94);for(let j=0;j<cuts.length-1;j++){const poly=traceTerraceBand(k,cuts[j],cuts[j+1]);if(!poly||area(poly)<58)continue;const c=centroid(poly),stage=(k+j)%4;fields.push({id:`TF${String(id++).padStart(3,'0')}`,kind:'terrace',k,poly,centroid:c,area:area(poly),bed:k*1.48,stage,stageName:['transplanted','tillering','heading','ripening'][stage],waterDepth:stage<2?.035:0});}}return fields;}
const terraceFields=buildTerraceFields(),allFields=[...terraceFields,...plainFields];
function fieldAt(x,z){return plainFieldAt(x,z)||terraceFields.find(f=>pointInPoly(x,z,f.poly))||null;}
function riverZ(x){return 151+11*Math.sin((x+35)*.019)+4*Math.sin(x*.053);}
const sourcePath=[[-6,-205],[-18,-188],[-34,-171],[-51,-151],[-68,-131],[-82,-113]],mainCanal=[[-102,-108],[-72,-104],[-42,-100],[-10,-97],[25,-92],[60,-86],[88,-78]],branchA=[[-66,-103],[-70,-78],[-63,-50],[-58,-21],[-45,12]],branchB=[[-12,-97],[-20,-68],[-14,-38],[-8,-8],[5,22]],branchC=[[55,-87],[48,-58],[55,-28],[48,0],[52,27]],plainCanal=[[5,22],[8,44],[2,67],[13,93],[20,121],[24,143]];
function makeFeeder(id,x0,index){const p=[];for(let i=0;i<=18;i++){const z=mix(20,134,i/18),q=warpPoint(x0,z);p.push(q);}return{id,p,w:.52,y0:1.10-index*.035,y1:.43-index*.025};}
const feederCanals=[makeFeeder('FDR0',-68,0),makeFeeder('FDR1',-8,1),makeFeeder('FDR2',58,2)];
function dseg(px,pz,a,b){const dx=b[0]-a[0],dz=b[1]-a[1],t=clamp(((px-a[0])*dx+(pz-a[1])*dz)/(dx*dx+dz*dz),0,1),x=mix(a[0],b[0],t),z=mix(a[1],b[1],t);return{d:Math.hypot(px-x,pz-z),t,x,z};}
function polyProjection(x,z,p){const lens=[];let total=0;for(let i=0;i<p.length-1;i++){const l=Math.hypot(p[i+1][0]-p[i][0],p[i+1][1]-p[i][1]);lens.push(l);total+=l;}let best={d:1e9,t:0},acc=0;for(let i=0;i<p.length-1;i++){const q=dseg(x,z,p[i],p[i+1]);if(q.d<best.d)best={d:q.d,t:(acc+q.t*lens[i])/(total||1),x:q.x,z:q.z};acc+=lens[i];}return best;}
function nearestPolyPoint(poly,target){let best=null;for(let i=0;i<poly.length;i++){const q=dseg(target[0],target[1],poly[i],poly[(i+1)%poly.length]);if(!best||q.d<best.d)best=q;}return[best.x,best.z];}
function terrainNoCuts(x,z){let nat=baseNatural(x,z);const tm=terraceMask(x,z);if(tm>.001)nat=mix(nat,terraceInfoFromNatural(nat,x,z).y,tm);const ff=plainFieldAt(x,z);if(ff)nat=mix(nat,ff.bed,.97);return nat;}
const channelSpecs=[{id:'source',p:sourcePath,w:1.0,y0:18.1,y1:16.35},{id:'main',p:mainCanal,w:1.28,y0:16.0,y1:10.45},{id:'branchA',p:branchA,w:.82,y0:14.4,y1:.72},{id:'branchB',p:branchB,w:.82,y0:11.5,y1:.68},{id:'branchC',p:branchC,w:.82,y0:10.5,y1:.62},{id:'plain',p:plainCa