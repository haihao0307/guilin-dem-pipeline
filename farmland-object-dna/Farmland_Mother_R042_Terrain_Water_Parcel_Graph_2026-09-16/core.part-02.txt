nal,w:1.0,y0:1.10,y1:-.38},...feederCanals];
function channelSurface(spec,t){return mix(spec.y0,spec.y1,t);}
function terrainY(x,z){let y=terrainNoCuts(x,z);for(const spec of channelSpecs){const q=polyProjection(x,z,spec.p),d=q.d;if(d<spec.w+1.08){const bed=channelSurface(spec,q.t)-.16,cut=mix(bed,y,sm(spec.w,spec.w+1.08,d));y=Math.min(y,cut);}}const rz=riverZ(x),d=Math.abs(z-rz),rw=10.5+1.5*Math.sin(x*.024);if(z>125)y=mix(-.72,y,sm(rw,rw+5,d));return y;}

function edgeMidpoint(e){return[(e.a[0]+e.b[0])*.5,(e.a[1]+e.b[1])*.5];}
function buildPlainAdjacency(){const byId=Object.fromEntries(plainFields.map(f=>[f.id,f])),adj=new Map();for(const f of plainFields)adj.set(f.id,[]);for(const e of plainEdges){if(e.owners.length!==2)continue;const[a,b]=e.owners,m=edgeMidpoint(e);adj.get(a).push({id:b,edge:e,mid:m});adj.get(b).push({id:a,edge:e,mid:m});}return{byId,adj};}
function nearestFeeder(point){let best=null;for(const f of feederCanals){const q=polyProjection(point[0],point[1],f.p);if(!best||q.d<best.d)best={id:f.id,point:[q.x,q.z],d:q.d};}const q=polyProjection(point[0],point[1],plainCanal);if(!best||q.d<best.d)best={id:'plain',point:[q.x,q.z],d:q.d};return best;}
function buildIrrigationGraph(){const nodes=[{id:'SOURCE',kind:'source',level:18.2},{id:'RIVER',kind:'receiver',level:-.72},{id:'branchA',kind:'canal',level:14.4},{id:'branchB',kind:'canal',level:11.5},{id:'branchC',kind:'canal',level:10.5},{id:'plain',kind:'canal',level:1.1}],edges=[];for(const f of feederCanals){nodes.push({id:f.id,kind:'canal',level:f.y0});edges.push({id:`SOURCE-${f.id}`,from:'SOURCE',to:f.id,kind:'trunk',points:[[0,18],f.p[0]]});}for(const f of allFields)nodes.push({id:f.id,kind:'field',level:f.bed,area:f.area});const{byId,adj}=buildPlainAdjacency(),incoming=new Map();for(const f of plainFields){const candidates=(adj.get(f.id)||[]).map(a=>({a,n:byId[a.id],dz:byId[a.id].centroid[1]-f.centroid[1]})).filter(v=>v.dz>1.0).sort((u,v)=>(u.dz+Math.abs(u.n.centroid[0]-f.centroid[0])*.08)-(v.dz+Math.abs(v.n.centroid[0]-f.centroid[0])*.08));if(candidates.length){const pick=candidates[0],m=pick.a.mid,c1=f.centroid,c2=pick.n.centroid,p1=[mix(m[0],c1[0],.10),mix(m[1],c1[1],.10)],p2=[mix(m[0],c2[0],.10),mix(m[1],c2[1],.10)];f.outlet={field:pick.n.id,point:m};if(!pick.n.inlet)pick.n.inlet={field:f.id,point:m};edges.push({id:`${f.id}-${pick.n.id}`,from:f.id,to:pick.n.id,kind:'field-spill',points:[p1,m,p2]});incoming.set(pick.n.id,(incoming.get(pick.n.id)||0)+1);}else{const p=nearestPolyPoint(f.poly,[f.centroid[0],riverZ(f.centroid[0])]);f.outlet={river:true,point:p};edges.push({id:`${f.id}-RIVER`,from:f.id,to:'RIVER',kind:'field-drain',points:[p,[p[0],riverZ(p[0])]]});}}
for(const f of plainFields){if((incoming.get(f.id)||0)>0)continue;const feed=nearestFeeder(f.centroid),p=nearestPolyPoint(f.poly,feed.point);f.inlet={canal:feed.id,point:p};edges.push({id:`${feed.id}-${f.id}`,from:feed.id,to:f.id,kind:'field-inlet',points:[feed.point,p]});}
const byK=new Map();for(const f of terraceFields){if(!byK.has(f.k))byK.set(f.k,[]);byK.get(f.k).push(f);}const levels=[...byK.keys()].sort((a,b)=>b-a),terraceIncoming=new Map();for(let li=0;li<levels.length;li++){const group=byK.get(levels[li]),down=li<levels.length-1?byK.get(levels[li+1]):null;for(const f of group){if(down&&down.length){let target=down[0],best=Infinity;for(const d of down){const score=Math.abs(d.centroid[0]-f.centroid[0])+.15*Math.abs(d.centroid[1]-f.centroid[1]);if(score<best){best=score;target=d;}}const p1=nearestPolyPoint(f.poly,target.centroid),p2=nearestPolyPoint(target.poly,f.centroid);f.outlet={field:target.id,point:p1};if(!target.inlet)target.inlet={field:f.id,point:p2};edges.push({id:`${f.id}-${target.id}`,from:f.id,to:target.id,kind:'terrace-spill',points:[p1,p2]});terraceIncoming.set(target.id,(terraceIncoming.get(target.id)||0)+1);}else{const p=nearestPolyPoint(f.poly,[f.centroid[0],20]);f.outlet={canal:'plain',point:p};edges.push({id:`${f.id}-plain`,from:f.id,to:'plain',kind:'terrace-drain',points:[p,[p[0],20]]});}}}
for(const f of terraceFields){if((terraceIncoming.get(f.id)||0)>0)continue;const branch=f.centroid[0]<-28?'branchA':f.centroid[0]>32?'branchC':'branchB',p=nearestPolyPoint(f.poly,[f.centroid[0],f.centroid[1]-4]);f.inlet={canal:branch,point:p};edges.push({id:`${branch}-${f.id}`,from:branch,to:f.id,kind:'terrace-inlet',points:[[f.centroid[0],f.centroid[1]-4],p]});}
edges.push({id:'SOURCE-branchA',from:'SOURCE',to:'branchA',kind:'trunk',points:[sourcePath.at(-1),branchA[0]]},{id:'SOURCE-branchB',from:'SOURCE',to:'branchB',kind:'trunk',points:[sourcePath.at(-1),branchB[0]]},{id:'SOURCE-branchC',from:'SOURCE',to:'branchC',kind:'trunk',points:[sourcePath.at(-1),branchC[0]]},{id:'plain-RIVER',from:'plain',to:'RIVER',kind:'drain',points:[plainCanal.at(-1),[plainCanal.at(-1)[0],riverZ(plainCanal.at(-1)[0])]]});return{nodes,edges};}
const irrigation=buildIrrigationGraph();
function validateIrrigation(){const nodeSet=new Set(irrigation.nodes.map(n=>n.id)),out=new Map(),inc=new Map(),errors=[];for(const e of irrigation.edges){if(!nodeSet.has(e.from)||!nodeSet.has(e.to))errors.push(`missing-node:${e.id}`);if(!out.has(e.from))out.set(e.from,[]);out.get(e.from).push(e.to);if(!inc.has(e.to))inc.set(e.to,[]);inc.get(e.to).push(e.from);}const reaches=(a,b)=>{const seen=new Set([a]),q=[a];while(q.length){const n=q.shift();if(n===b)return true;for(const m of out.get(n)||[])if(!seen.has(m)){seen.add(m);q.push(m);}}return false;};for(const f of allFields){if(!(inc.get(f.id)||[]).length)errors.push(`no-inlet:${f.id}`);if(!(out.get(f.id)||[]).length)errors.push(`no-outlet:${f.id}`);if(!reaches('SOURCE',f.id))errors.push(`no-source:${f.id}`);if(!reaches(f.id,'RIVER'))errors.push(`no-river:${f.id}`);}return{ok:errors.length===0,errors,fieldCount:allFields.length,plainCount:plainFields.length,terraceCount:terraceFields.length,edgeCount:irrigation.edges.length};}
const irrigationValidation=validateIrrigation();
const shelterDefs=[[-104,36],[-42,61],[37,83],[102,108],[-41,4]].map((p,i)=>({id:`H${i+1}`,x:p[0],z:p[1]}));
const workFields=plainFields.filter(f=>f.stage<2).sort((a,b)=>a.id.localeCompare(b.id)),farmerDefs=[];for(let i=0;i<20;i++){const f=workFields[(i*7+3)%workFields.length];farmerDefs.push({id:`P${i+1}`,fieldId:f.id,x:f.centroid[0],z:f.centroid[1],role:['transplant','bund-walk','water-gate','bundle'][i%4],hat:true,phase:i/20});}
const buffaloDefs=[];for(let i=0;i<8;i++){const f=workFields[(i*11+5)%workFields.length];buffaloDefs.push({id:`B${i+1}`,fieldId