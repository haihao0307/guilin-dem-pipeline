name: Ocean Island R017 legacy publisher held for R018 maintenance

"on":
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  holding:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "R017 publisher intentionally held while R018 is released"
