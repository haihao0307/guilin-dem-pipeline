name: Wenzhou R3.3 GEM active-fault context

on:
  push:
    branches:
      - feature/wenzhou-r33-environmental-data-intake-20260908
    paths:
      - .github/workflows/wenzhou-r33-gem-active-faults.yml
      - experiments/wenzhou-r33-environmental-data-intake/scripts/acquire_gem_faults_r33.py
  workflow_dispatch:

permissions:
  contents: read

jobs:
  acquire:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Install geospatial tools
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y gdal-bin python3-gdal
          python3 -m pip install --disable-pip-version-check requests
          ogr2ogr --version

      - name: Acquire fixed source and make Wenzhou subsets
        run: |
          set -euo pipefail
          python3 experiments/wenzhou-r33-environmental-data-intake/scripts/acquire_gem_faults_r33.py \
            --output "${{ github.workspace }}/artifacts/wenzhou-r33-gem-active-faults"

      - name: Verify payload hashes
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import hashlib, json
          root = Path('artifacts/wenzhou-r33-gem-active-faults')
          manifest = json.loads((root / 'PAYLOAD_MANIFEST.json').read_text(encoding='utf-8'))
          errors = []
          for item in manifest['payloads']:
              path = root / item['path']
              if not path.is_file():
                  errors.append([item['path'], 'missing'])
                  continue
              if path.stat().st_size != item['bytes']:
                  errors.append([item['path'], 'size'])
                  continue
              if hashlib.sha256(path.read_bytes()).hexdigest() != item['sha256']:
                  errors.append([item['path'], 'sha256'])
          result = {'passed': bool(manifest.get('passed')) and not errors, 'checked': len(manifest['payloads']), 'errors': errors}
          (root / 'reports' / 'MANIFEST_VERIFICATION.json').write_text(json.dumps(result, indent=2) + '\n', encoding='utf-8')
          print(json.dumps(result, indent=2))
          if not result['passed']:
              raise SystemExit(2)
          PY

      - name: Upload source, license and subsets
        uses: actions/upload-artifact@v4
        with:
          name: wenzhou-r33-gem-active-faults
          path: artifacts/wenzhou-r33-gem-active-faults
          if-no-files-found: error
          retention-days: 14
          compression-level: 6
