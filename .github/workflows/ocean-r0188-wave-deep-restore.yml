name: Ocean Mother R018.8 legacy publisher held

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  holding:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by ocean-r0188-release-recovery.yml"
