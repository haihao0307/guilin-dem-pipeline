name: KAOPU mediump coordinate N13

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-for-atomic-probe-update:
    runs-on: ubuntu-latest
    steps:
      - run: echo "N13 probe update staging only"
