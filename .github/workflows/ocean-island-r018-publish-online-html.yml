name: Ocean Island R018 verified online HTML

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  holding:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "R018 payload maintenance gate"
