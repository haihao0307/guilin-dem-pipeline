name: Ocean Island R018 publisher maintenance gate

"on":
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  holding:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "R018 payload update in progress"
