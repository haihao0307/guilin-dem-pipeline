# 桂林 12.5 米 DEM 重建源包

这是 2026-08-25 重新下载的桂林 DEM 源片，作为新的唯一重建输入。

## 内容

- 12 张 12.5 米单波段 DEM，均为 EPSG:32649 / WGS 84 UTM 49N
- 每张源片对应的 `.iso.xml` 元数据
- 源片 SHA256、范围、NoData 和首次拼接覆盖率清单
- 不包含网页派生高度图、侵蚀结果、材质或旧页面数据

## 当前拼接状态

首轮按源片仿射位置落到统一 12.5 米网格后，联合范围有效覆盖率约为 86.64%，仍有约 13.36% NoData/缺口。没有填补缺口，也不能把当前结果称为完整无缝拼接；缺口位置和范围记录在 `catalog/source_manifest.json` 及本地拼接清单中。

## 重建规则

1. 当前目录只使用本包中的 12 张新源片。
2. 旧的 `AP_10613_FBS_F0480_RT1.dem.tif` 已从当前 canonical 目录移除。
3. 原始 DEM 只读；拼接、网页和后续画线都是派生结果。
4. NoData 不自动填补，后续若要补全，追加真实源片后重新拼接。

校验：

```bash
python tools/verify_dem.py
```
