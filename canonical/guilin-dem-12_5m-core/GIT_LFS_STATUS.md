# Git LFS 状态

本目录的 12 张 `.dem.tif` 源片使用 Git LFS；对应的 `.iso.xml` 和清单文件使用普通 Git。

12 个 LFS 对象已上传。应在全新目录执行 `git lfs pull`，再运行 `tools/verify_dem.py` 复核源片。

旧的 `AP_10613_FBS_F0480_RT1.dem.tif` 不属于本次重建源包，已从当前目录移除。
