import {WeatherBridge} from './weather-bridge.mjs';
const $=id=>document.getElementById(id),q={version:'wenzhou-workbench-0.1.0-weather110',ready:false,checks:{files:0},errors:[],exchanges:0,sourceIdentityVerified:false,attachmentBytesCompared:false,sharedDepth:false,visualApproved:false,productionApproved:false};
window.__WZ_WORKBENCH__=q;let bridge,build,lastFrame=null,stopped=false;
function fail(e){let s=String(e.stack||e);q.errors.push(s);q.ready=false;$('errors').style.display='block';$('errors').textContent=s;console.error(e);}
addEventListener('error',e=>fail(e.error||e.message));addEventListener('unhandledrejection',e=>fail(e.reason));
async function hash(b){return[...new Uint8Array(await crypto.subtle.digest('SHA-256',b))].map(x=>x.toString(16).padStart(2,'0')).join('');}
async function json(path){let r=await fetch(path,{cache:'no-cache'});if(!r.ok)throw Error(path+' HTTP '+r.status);return r.json();}
function terrain(){return $('terrain').contentWindow?.__WZ_API__;}
async function waitReady(){let start=performance.now();while(performance.now()-start<180000){let tw=$('terrain').contentWindow,ww=$('weather').contentWindow;let tq=tw?.__WZ_FULL__,wq=ww?.WeatherMother?.qa;if(tq?.errors?.length||wq?.errors?.length)throw Error(JSON.stringify({terrain:tq?.errors,weather:wq?.errors}));if(tq?.ready&&wq?.ready&&wq?.frames>0){try{return bridge.sample();}catch(e){if(!/TRANSITION|NOT_READY/.test(e.message))throw e;}}await new Promise(r=>setTimeout(r,150));}throw Error('工作台启动超时，未宣告就绪');}
function loop(){try{let f=bridge.sample();lastFrame=f;q.exchanges++;q.lastFrame=f;q.bridgeStatus='receiving';if($('couple').checked)terrain()?.applyWeatherFrame(f);else terrain()?.applyWeatherFrame(null);let t=terrain()?.state();q.terrain=t;q.weather=$('weather').contentWindow.WeatherMother.qa;q.imageRequests=performance.getEntriesByType('resource').filter(x=>/\.(png|jpe?g|webp|ktx|dds)(\?|$)/i.test(x.name)).length;
$('mapState').textContent=t?.ready?'WebGL2 已绘制':'等待地图';$('weatherState').textContent=f.identity.kind+' · '+f.identity.weather;
$('metrics').textContent=`来风 ${f.wind.speedMps.toFixed(1)} m/s · 云速 ${f.cloud.speedMps.toFixed(1)} m/s · 天气 ${f.clock.simulationSeconds.toFixed(1)} s`;
$('status').textContent=$('couple').checked?(t?.weather.active?'风场、日光、演示时钟已传入地图；潮位与地形保持独立。':'环境数据已读取；当前检查模式固定照明。点击地图内“天气联动”查看响应。'):'联动已关闭；地图不再消费天气状态。';
}catch(e){q.bridgeStatus=e.message;terrain()?.applyWeatherFrame(null);$('status').textContent=/TRANSITION|NOT_READY/.test(e.message)?'天气生成或切换中，暂时停止向地图传递环境。':e.message;if(!/TRANSITION|NOT_READY/.test(e.message)&&!stopped){stopped=true;fail(e);}}if(!stopped)requestAnimationFrame(()=>setTimeout(loop,120));}
function events(){document.querySelectorAll('[data-layout]').forEach(b=>b.onclick=()=>{$('frames').className='frames '+b.dataset.layout;document.querySelectorAll('[data-layout]').forEach(x=>x.classList.toggle('active',x===b));q.layout=b.dataset.layout;});$('case').onchange=e=>{try{bridge.control('weather',e.target.value);}catch(e){fail(e);}};$('pause').onclick=()=>bridge.pause();$('play').onclick=()=>bridge.play();$('couple').onchange=()=>{if($('couple').checked){terrain()?.setMode('environment');if(lastFrame)terrain()?.applyWeatherFrame(lastFrame);}else terrain()?.applyWeatherFrame(null);};$('inspect').onclick=()=>{$('receiptText').textContent=JSON.stringify({build,current:q,terrainHistory:terrain()?.history()},null,2);$('receipt').showModal();};$('close').onclick=()=>$('receipt').close();}
async function init(){build=await json('./BUILD.json');if(build.version!==q.version||build.visualApproved||build.productionApproved)throw Error('工作台构建身份不符');q.sourceCommit=build.sourceCommit;
const names=Object.keys(build.files).filter(n=>n.startsWith('modules/weather-mother/'));if(names.length!==12)throw Error('天气包文件数量不符');
await Promise.all(names.map(async n=>{const r=await fetch(n,{cache:'no-cache'});if(!r.ok)throw Error(n+' HTTP '+r.status);let b=await r.arrayBuffer(),x=build.files[n];if(b.byteLength!==x.bytes||await hash(b)!==x.sha256)throw Error('天气文件身份不符 '+n);q.checks.files++;}));q.sourceIdentityVerified=true;
const handoff=await json('./modules/weather-mother/HANDOFF.json');q.weatherCases=Object.keys(handoff.weatherCases).length;q.cloudGenera=handoff.cloudGenera.length;q.declaredWeatherRuntime=handoff.runtimeVersion;
$('case').replaceChildren(...Object.entries(handoff.weatherCases).map(([value,label])=>{let o=document.createElement('option');o.value=value;o.textContent=label;return o;}));$('case').value='coast';
$('terrain').src='./terrain/index.html';$('weather').src='./modules/weather-mother/index.html?weather=coast';bridge=new WeatherBridge($('weather'));events();let f=await waitReady();q.ready=true;$('buildId').textContent=build.version+' · '+build.sourceCommit.slice(0,10);for(let id of['case','pause','play'])$(id).disabled=false;
terrain().applyWeatherFrame(f);terrain().setMode('environment');window.WenzhouWorkbench={bridge,terrain,inspect:()=>q,getBuild:()=>build};loop();}
init().catch(fail);
