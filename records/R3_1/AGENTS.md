# 温州 R3.1：海岸、河道和体积

用户要求继续温州自身工作；不参与其他项目清理或将其作为停工条件。执行本目录 Mother 对象与公网原件，禁止读取、调用、恢复撤销流程。

保留已固定 R3 的全部公网文件。使用现有 Site 的新增 `/r3-1/` 路径交付；不覆盖 `/r3/`。

本轮范围：如实报告对象/规则、采样资料、网页、压缩包与完整无损源资料的各自体积；按可信海陆边界取消海域中的伪地形方框；标注有来源的河道。不能把低高程或 NoData 当海岸，不能将中心线/地图水体当实测水位、河床或水文连接。

离地1.6m作为用户指定的后续相机目标，本轮不据此伪造米级地形或过度放大实测精度。向小妈学习并应用温州相关知识，不设逐步回签门槛。
