# V0.2.2 build trigger

Build the physical beach correction from the current branch head. Do not count this marker as production evidence; the generated runtime, numerical checks and real-browser captures are the evidence.
