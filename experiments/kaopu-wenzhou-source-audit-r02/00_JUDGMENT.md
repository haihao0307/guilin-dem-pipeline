# 靠谱来源核查 R02 — 2026-09-11

状态：候选审计；不是正式靠谱 R2。小妈本轮完成来源核查、反例检查与 Judgment。

## 承接点

- R1 语义冻结：`cd9160ce90cd1c6c6a49f4fbb2ae1f2c55470330`，保持不变。
- 全量交接：`f254721b7e3e23cb35b7b660fa9441dc6cef9796`。
- 龙湾 R01：`2af08d75f7011eb3d7a844ee9f552211964ec7ab`。原样保留。
- 学习飞轮查询头：`9197c21b0a2d9d6aa9b6f698ba984fff8d5649fd`；R29 是独立候选增量，不因编号较大自动成为语义冻结。

## 来源回读

2026-09-11 回读 [Frontiers 原始研究论文](https://www.frontiersin.org/journals/marine-science/articles/10.3389/fmars.2026.1898828/full)。表 2 支持 R01 的两个极值及差值；表 6 支持其两类趋势和论文推导的陆地运动数值。第 2.1 节描述验潮记录及卫星网格数据的来源与处理。上述结果属于论文报告，未独立重算。原始字节未在本轮取得；因此没有填写原始数据校验和。

MDPI 补充背景页本轮读取失败，不用于新增结论。限定 PSMSL、Copernicus 的公开检索未返回可用于锁定该站和论文所用精确产品的有效结果；不能据此推断数据不存在。未检索中国网站，未联系作者或向任何人发送消息。

## 判断与修正

R01 的 23 项检查可验证结构和部分算术，不能证明原始观测已重算。不同标签或传感方式不足以证明独立；两种观测都经同一论文引用，也不足以证明底层物理观测相依。新的审计输出明确保留独立性为未知。

新增脚本识别本案例 DOI 与出版商链接的同源副本，并输出来源缺口。它是保守的缺口检查器，不是通用来源鉴真系统，也没有完整证据依赖图。即使输入自称成功，也不会输出物理验证或生产完成。

一次实际独立 AI 只读审查已完成，复验 R01 23 项与初版 R02 6 项检查。采纳三点：逐根明确旧 rootStatus 的独立性自述未验证；参考系、量值与空间支撑未对齐时不把数值差异当冲突；输出声明为固定案例的缺口快照，不能自动判断未来补齐的资料。增加两项对应回归测试后为 8 项。AI 审查属于候选代码审查，不新增物理观测根；没有声称调用不同供应商模型。

## 工作队列

1. Continue：锁定龙湾站元数据、原始验潮序列及处理历史；不从图上猜精确坐标。
2. Continue：锁定论文实际使用的卫星产品、版本、采样点和时间窗口；不以分辨率相同的产品替代。
3. Park：原始趋势、相位和残差重算，直到前两项有可用数据与来源链。
4. Continue：将“结构成功 / 原始数据重算 / 独立性 / 人工接受”分开记录；此方法可供其他 Mother 使用，但本轮未修改生产 Mother。

保留 Unknown，不生成观测来填空。最终阶段接受仍由用户决定。

## 总指挥运行职责（用户本轮补充）

小妈持续学习、指导其他 Master、与可用 AI 交流成果并推进整体项目；龙湾只是本轮有界案例，不替代总协调职责。用户稍后提供的新知识进入学习队列。

已核实既有 `KAOPU Learning Flywheel` 自动任务启用，周期为每两小时；沿用此任务，不新建重复循环。每轮读取最新 GitHub 状态，选择一个高价值问题，继承成熟成果，独立质疑，运行有判别力的检查，再整理相关 Master 的行动与反馈。多个 AI 的一致意见不能当作独立物理证据。无可用 AI 接口或没有实际交流时，必须如实记录。

本轮方法对接既有 `MOTHER_ROUTING.json` 中 R05/R06 的证据依赖规则。新增价值是龙湾具体反例与可执行审计，而非重复发明独立性原则。

## 本轮验证

运行原 R01 `verify_fixture.py`；运行本目录 `python -m unittest discover -s .`；运行 `python audit_sources.py` 重算审计结果。测试仅覆盖明确的误晋级与同源复制风险，不代表物理世界验证。
