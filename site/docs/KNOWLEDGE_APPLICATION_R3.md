# 小妈知识在温州 R3 的实际应用

依据：同目录 XIAOMA_3D_KNOWLEDGE_R3.md，WZ-3D-KNOWLEDGE R0.1。用户明确恢复温州生产；此轮仅处理温州。

| 知识 | 已落实的位置与实测 |
| --- | --- |
| 哈希恢复不等于物理精度 | 全新 prepare_dem.py 逐块核对 1219 个条目，再核整网格行主序 SHA；DEM_DECODE_QA_R3.json 保留独立物理精度未知 |
| 格心不等于像元外角 | 坐标带 +0.5；首末格心、域外拒绝和投影/显示往返核对。实测双精度往返最大误差为 0，仅限所测构造 |
| 双线性不等于三角形 | 持久规范为原 12.5m 格心双线性曲面；显示按粗化样本生成临时三角面，另存全保留域近似误差界 |
| 不跨缺测连面 | 每个粗显示单元检查其覆盖的所有原始样本，包括内部；移动端进一步粗化继承子单元有效性 |
| 原查询与新曲面求值分别保留 | 12 条原像元高程、原坐标、方法保留；新增四角、uv、规则和曲面高程。01 原值 801m，曲面约 802.0m，未强行改成相等 |
| 显示锚定不是物理连接 | 查询标记依实际显示三角面放置，使用视觉抬升；不改变世界位置，不声明组成或物理连接。曲面缺支撑时不造落点 |
| 坐标父架与组成分离 | DEM、派生曲面、相互重叠的显示片、查询位置各有不同身份和关系，EPSG:32651 不充当组成关系 |
| 复位恢复未增强状态 | 默认 α=1、各轴统一米转千米；3×/6×明示，复位恢复 α=1 和当前范围拟合相机 |
| 12点不代表全域 | 山地近景来自整幅数据最大高程所在区域；全域按完整格网范围显示；12点仅作局部查询 |

SURFACE_QA_R3.json 记录 4803 个可用支撑格心、4648 个共享边检查（含编码块接缝）、296 个最外格心请求、336 个有效零值和全部 12 个查询的核对。

显示误差界针对所有实际保留的显示单元：先穷举覆盖的原格网节点，取得原双线性曲面与粗双线性曲面的最大差；两者之差在每个原单元仍是双线性，极值位于角点。再加粗单元双线性片与两三角片的代数上界。这个和是保守界，不是精确的最大三角误差，也不包括 GPU 坐标舍入、被排除的边界或物理测量误差。没有用随机抽样冒充全域最大误差。

当前按单一显示片切换范围；不同时拼接不同 LOD，因此没有用裙边掩盖混合 LOD 接缝。每片内部跨原始编码边界取同一全局网格。每次切换销毁原显示缓冲；只缓存测量样本和有效支撑。

## 新解码合同

固定载荷和索引先校验完整 SHA。索引中的每条记录前 52 字节：前 20 字节按 little-endian `<4H3I` 解释为 tileRow、tileCol、真实 height、真实 width、validPixels、maskBytes、dataBytes，后 32 字节为索引 rawSha256 对应的摘要。

随后先读取 maskBytes 字节的 zlib 掩膜，再读取 dataBytes 字节的 zlib uint16 残差。掩膜按 little bit order 解包并截到真实块面积。残差以 little-endian uint16、行主序解释，边界预测初值为零；沿两轴前缀求和后取模 65536，恢复 uint16 数值位型。块哈希覆盖恢复后的 uint16 字节加解压后的位打包 mask 字节。通过后再以 int16 解释，并将 mask=false 恢复成 -32768。空槽初始化为 NoData；最后整网格哈希验证缺块、拼接或行列次序错误，不能靠每块单独通过来代替。

整数零和负值均保留；损坏/截短压缩段会在解压、尺寸或哈希阶段失败。全量临时解码数组位于站点外，不上传为渲染顶点数据。页面交付的是采样测量和规范，顶点、法线与三角索引运行时生成。
