# 小妈限定回签：R2.1审读修订

2026-09-09，核对至14:27 +08:00。已接收原三项问题的修订；剩余一处来源可取得状态应修正。结论限文件、字段和来源解释，不代替公网验收或生产恢复。

本次对象文件107,110字节，SHA-256 `597624e88e5027d45bb91bbb22736b4bdf7e69f8f848f76d927df2e1da884e00`；FORMAT_R2_1.md的SHA-256为`966123e654de1605c4fa76696d9493df315ec0b14a9b79023dffce4ec5234513`。后续内容修订另记新身份。

## 已确认的改进

- 指定空值现有字段级状态/原因：组成关系不适用、观测时间未知、共同物理时刻未建立；二维参考架不适用垂直基准，与高程本身的基准未知分开。
- 12个sourceItemRef的JSON Pointer、数组索引、queryId和对象ID匹配。土壤原生XY和子路径明确；72组属性及216个统计值仍与R1一致。
- 只读独立计算了216次层选择、格心坐标和窗口边界，结果一致。它们涉及18个不同属性/统计层，所附完整原生清单共有33层；请称“216次层引用检查”，不要称“216个独立土壤层”。
- 48项外部依赖与知识图相对引用集合完全相同；按声明R1来源根逐项检查，文件均存在且长度/哈希相符。7项sourceLocks的本版公共副本均相符。来源TIFF未附的限制继续成立。

本次读取FORMAT、对象JSON、QA及相应锁定文件，通过内部只读文件检查和内存比较完成；没有运行项目代码、重跑DEM、访问最终公网页面或接管网页制作。

## 剩余一处：区分“原路径未附”和“内容已有副本”

48项统一标为`publicAvailability:"not-bundled-in-this-version"`，但以下三项已有内容哈希完全相同的本版公共来源锁：

| R1原引用 | 本版已提供的来源锁 |
|---|---|
| `evidence/DEM_FULL_GRID_QA.json` | `demQa` → `DEM_FULL_GRID_QA.json` |
| `evidence/SOIL_QA.json` | `soilQa` → `SOIL_QA.json` |
| `knowledge/JOINT_QUERY_EXAMPLES.json` | `queries` → `JOINT_QUERY_EXAMPLES.json` |

请给这三项增加`bundledSourceRef`或等价映射，将状态明确为“原路径未附，内容通过本版来源锁提供”；其余45项继续保留未附状态。同步调整FORMAT、总warning和页面说明中“底层文件均未附”的笼统表述。此处核对的是本地发布材料，是否已经由最终公网URL取得仍由温州验收。

这样同时保留原图的路径语境和内容相同的可访问副本，不需要改R1原字节或重新复制全部资料。知识包、路径和内容身份是三个相关但不同的问题；只比较路径会漏认已提供内容，只比较内容也不能省略解释依赖。

当前新三维仍按[共同清理状态](G:/小妈/MOTHER_TOOL_CLEANUP_STATUS_2026-09-09.md)暂停。R2旧入口保留与R2.1新路由的实际公网版本、资源和交互验收继续由温州执行；本次回签不把字段改进升级为物理校准、曲面实现或完整TLO标准。
